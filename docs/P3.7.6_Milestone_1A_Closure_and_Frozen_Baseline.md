# Sprint P3.7.6 — Milestone 1A Closure & Frozen Baseline

**Sprint type:** Repository Acceptance / Frozen Baseline
**Repository:** `ai-quality-engineering`
**Accepted engineering state:** commit `fd6c4c7` — *feat(retrieval): implement canonical document marking (P3.7.5)*, branch `main`, working tree clean
**Scope:** Acceptance only. No implementation, no governance, no architectural change, no Milestone 1B capability.

This sprint **certifies a repository state already achieved.** It creates nothing.

---

## 1. Milestone summary

Milestone 1A set one gating question, and answered it:

> **Can we deterministically trace every answer back to verified knowledge, without relying on any probabilistic AI component?**
>
> **Yes — by demonstration**, not by assertion. Every statement the pipeline emits carries a chunk id, a document id and document-frame character offsets, and each statement's text is byte-identical to its supporting span.

The milestone built an evaluation lab before the system it measures, and proved the lab deterministic before allowing anything probabilistic near it. What it delivered is recorded in the authorities cited in §3 and is **not restated here** — per Work Package 3, this document freezes by reference rather than by duplication.

**Milestone 1A closes with its Definition of Done annotated, not satisfied.** Five of seven items are met; two are annotated with the reason they are not. That distinction is the milestone's most important governance property and is preserved verbatim at `docs/MILESTONE_1A.md` — *Repository Owner Scope Ruling (P3.7.3)*.

---

## 2. Completed sprint chronology

**57 commits.** The closure phase, in order:

| Sprint | Commit | Outcome |
|---|---|---|
| P3.5.1 | `dfbc86b` | Generation Contract frozen |
| P3.5.2 | `17d077b` | Deterministic Generation runtime |
| P3.6.0 | `b50e45f` | Deterministic CLI integration |
| **P3.7.0** | `8e73173` | Manual review evidence — 705-line verbatim transcript |
| **P3.7.1** | `d9a6db4` | Manual review assessment — 24 PASS / 0 FAIL, zero backlog items |
| **P3.7.2** | `8b2d387` | Governance synchronization — 16 modifications, every one traced |
| **P3.7.3** | `180dcdc` | Repository Owner Constitutional Decision — 51 capabilities allocated |
| **P3.7.4** | `8dade5b` | Repository authority synchronization — A1–A8 discharged or withheld |
| **P3.7.5** | `fd6c4c7` | Canonical Document Marking — RO-01 discharged |
| **P3.7.6** | *this document* | Milestone 1A closure & frozen baseline |

---

## 3. Frozen Baseline Statement

> **The Milestone 1A frozen baseline is the accepted repository state at commit `fd6c4c7`.**
>
> Subsequent milestones **SHALL extend** this baseline. They **SHALL NOT redefine** it.

**On the closure commit.** Work Package 3 defines the baseline as the state at *"the Milestone 1A closure commit."* That commit does not yet exist — it is the commit that will add this document. Since this sprint changes no engineering artifact, the closure commit's tree is identical to `fd6c4c7` in every respect except the presence of this report. **The baseline is therefore anchored to `fd6c4c7`**, the last commit containing engineering content, and the closure commit inherits it unchanged. Anchoring to a commit that does not yet exist would freeze a state no one has verified.

### 3.1 Frozen repository authorities

| Authority | Digest (SHA-256, first 12) | Status |
|---|---|---|
| `sample_rag/knowledge_manifest.json` | `584369393c7d` | **Frozen** — canonical designation added at P3.7.5 |
| `sample_rag/chunks.json` | `323723b4fe82` | **Frozen** — unchanged across six sprints |
| `datasets/golden/resume_facts.json` | `d5035f4013fc` | **Frozen** — unchanged across six sprints |
| `datasets/golden/resume_qa_pairs.json` | `c8c8f120f423` | **Frozen** — unchanged across six sprints |
| `datasets/golden/resume_evidence_trace.json` | `f45c2c2f5f41` | **Frozen** — unchanged across six sprints |

Four of the five digests are identical to those recorded by P3.5.2 §1, P3.6.0 §1, P3.7.1 §1, P3.7.2 §1.1 and P3.7.4 §1.1. The Manifest digest changed exactly once, at P3.7.5, for exactly one reason, recorded at `docs/P3.7.5_…` §3.2.

### 3.2 Frozen executable specifications

**372 specifications, 372 passing**, across fourteen modules:

| Module | Count | Module | Count |
|---|---|---|---|
| `test_generator.py` | 48 | `test_knowledge_source_construction.py` | 27 |
| `test_retrieval_diagnosis.py` | 47 | `test_cli.py` | 27 |
| `test_retrieval_evaluation.py` | 35 | `test_evidence_trace_dataset.py` | 25 |
| `test_retrieval_metrics.py` | 35 | `test_document_contract.py` | 22 |
| `test_knowledge_source_failures.py` | 29 | `test_chunker.py` | 17 |
| `test_cross_dataset_integrity.py` | 16 | `test_data_quality.py` | 14 |
| `test_golden_dataset.py` | 16 | `test_qa_pairs.py` | 14 |

### 3.3 Frozen governing authorities

Frozen **by reference**. Each remains the canonical statement of its own subject:

| Authority | Governs |
|---|---|
| `docs/MILESTONE_1A.md` | Milestone scope, Definition of Done, acceptance criteria, Architecture Freeze |
| `docs/GENERATION_CONTRACT.md` · `docs/DOCUMENT_CONTRACT.md` · `docs/CHUNK_CONTRACT.md` | The three frozen contracts |
| `docs/architecture.md` · `docs/roadmap.md` · `docs/altm.md` · `docs/glossary.md` | Architecture, milestone ordering, diagnostic model, vocabulary |
| `docs/P3.7.3_Repository_Owner_Constitutional_Decision.md` | Constitutional principles, milestone boundaries, capability allocation |
| `docs/DEFERRED_ITEMS_REGISTER.md` | The 51 deferred capabilities — canonical authority |
| `docs/ENGINEERING_TRACEABILITY_REGISTER.md` | Findings, ADRs, claim-to-specification mapping |

---

## 4. Final Verification Summary

Work Packages 1 and 6, measured at `fd6c4c7`.

| Verification | Result |
|---|---|
| Clean working tree | ✅ `git status --porcelain` empty |
| Latest committed repository state | ✅ `fd6c4c7` (P3.7.5) |
| Executable specifications | ✅ **372 / 372 passing** |
| Deterministic manifest digest | ✅ In-memory regeneration **byte-identical** to the committed artifact (`584369393c7d`) |
| Repository authority digests | ✅ All five as §3.1 |
| Canonical document designation | ✅ `bac26d34f834` (v2.3) `canonical: true`; `3f3797c1134c` (v2.2) `canonical: false` |
| `documents[].indexed` | ✅ `false` on both entries — **unchanged**, per **1B-11** |
| Reproducibility — CLI answer path | ✅ Byte-identical across runs (`b02e98aab0de…`) |
| Reproducibility — CLI abstain path | ✅ Byte-identical across runs (`03c858fe0246…`) |
| Evaluation | ✅ determinism, stable ordering, referential integrity, exhaustive classification, repository integrity — **all PASS** |
| Metrics | ✅ 10 validations — **all PASS**, including deterministic computation and reproducibility |
| Diagnosis (ALTM) | ✅ 11 validations — **all PASS**, including *unreachable stages absent* and *document identity integrity* |
| Evidence Trace behaviour | ✅ Dataset unchanged (`f45c2c2f5f41`); traceability intact |
| Authority consistency | ✅ No governance contradiction; register reconciles 51 of 51 |
| Implementation changes made by this sprint | ✅ **None** |

**Verification was performed without writing to the repository.** Manifest reproducibility was confirmed by rebuilding the manifest in memory and comparing its serialization to the committed artifact, rather than by regenerating in place — a sprint barred from modifying the manifest should not write to it even when the write would be byte-identical.

---

## 5. Repository Acceptance Statement

Assessed against `docs/P3.7.3_…`, `docs/P3.7.4_…`, `docs/P3.7.5_…` and the annotated Definition of Done. **No new acceptance criterion was introduced.**

```text
Repository Owner Acceptance — Milestone 1A

ACCEPTED

The deterministic engineering baseline established through Sprint P3.7.5
is accepted as the Milestone 1A frozen baseline.

Accepted:

• The North Star Question, answered yes by demonstration
• 372 executable specifications, all passing
• Five digest-verified repository authorities
• The deterministic pipeline: Knowledge -> Chunk Corpus -> Retriever
  -> Generator -> CLI
• Canonical document designation and its deterministic tie-break
• The Definition of Done as ANNOTATED, not as satisfied

Accepted with limitations recorded at Section 7.

This acceptance closes Milestone 1A. It resolves no deferred capability,
discharges no future authorization, and begins no subsequent milestone.
```

### 5.1 Definition of Done at acceptance

Reproduced from `docs/MILESTONE_1A.md` — *Repository Owner Scope Ruling (P3.7.3)*. **Not re-derived, and not re-judged.**

| Item | Status |
|---|---|
| All Runtime Pipeline build items (1–6) and Evaluation Asset build items (7–10) complete | **Annotated — not satisfied.** Build items 2 and 3 partially complete; completing clauses reassigned to Milestone 1B |
| All Functional and Architectural acceptance criteria checked | **Annotated — not satisfied.** Criteria F-1, F-2 and A-1 remain unchecked with evidence |
| The remaining five items | **Met** |

**Milestone 1A is accepted on what it demonstrated, not on a claim it did not earn.** `docs/P3.7.3_…` Decision 5 §5.3: *"A closure that claimed satisfaction would be the first false statement in this repository's governance record."* This acceptance does not make that statement.

*Naming note: acceptance criteria **F-1 / F-2 / A-1** belong to `docs/MILESTONE_1A.md`. Findings **F-1 … F-5** belong to `docs/P3.7.5_…` §8. Where a P3.7.5 finding is cited below it is written as **P3.7.5 F-n**.*

---

## 6. Authorization Discharge Register

Discharge is **evidence-backed, never inferred.** An authorization is discharged only where its scope was implemented, verified and accepted within Milestone 1A.

### 6.1 Discharged

| Artifact | Authorization | Completion evidence | Acceptance evidence | Status |
|---|---|---|---|---|
| P3.7.4 Repository Authority Synchronization | **A1** — Scope Ruling appended to `MILESTONE_1A.md` | `docs/P3.7.4_…` §3; append-only diff, 0 deletions | §5.1 above | **Discharged** |
| P3.7.4 | **A2** — Deferred Items Register as standalone document | `docs/DEFERRED_ITEMS_REGISTER.md`; `docs/P3.7.4_…` §2 | §3.3, §7.2 | **Discharged** |
| P3.7.4 | **A3** — `roadmap.md` §1 establishes Milestone 1B | `docs/P3.7.4_…` §4.2, all four requirements | §3.3 | **Discharged** |
| P3.7.4 | **A4** — `roadmap.md` §0 status correction (RO-04) | `docs/P3.7.4_…` §4.1 | §3.3 | **Discharged** |
| P3.7.4 | **A5** — `architecture.md` §5 `Generator` row (RO-03) | `docs/P3.7.4_…` §5.1 | §3.3 | **Discharged** |
| P3.7.4 | **A7** — milestone reference synchronization, six documents | `docs/P3.7.4_…` §5.1–§5.4 | §3.3 | **Discharged** |
| P3.7.4 | **A8** — sprint sequence correction, recorded not edited | `docs/DEFERRED_ITEMS_REGISTER.md` §9 | §3.3 | **Discharged** |
| P3.7.5 Canonical Document Marking | **RO-01** — canonical designation between v2.2 and v2.3 | `docs/P3.7.5_…` §3, §4, §6 | §4 (designation verified) | **Discharged** |
| P3.7.3 WP7 | Governance annotation of the Milestone 1A Definition of Done | A1, above | §5.1 | **Discharged** |
| P3.7.3 WP7 | Deferred capability allocation | A2, above; 51 capabilities | §7.2 | **Discharged** |
| P3.7.3 WP7 | Establishment of Milestone 1B | A3, above | §3.3, §8 | **Discharged** |
| P3.7.3 WP7 | Progression past the constitutional decision | P3.7.4 and P3.7.5, both committed | §2 | **Discharged** |

**12 authorizations discharged**, each with distinct completion and acceptance evidence.

### 6.2 Explicitly NOT discharged

Withheld because the scope was not implemented, or because discharge belongs to a future Repository Owner decision.

| Authorization | Why not discharged |
|---|---|
| **A6** — vector store implementation choice | **Conditional; no election was made.** `docs/P3.7.4_…` §5.5 verified that no authority was amended to adopt sqlite-vec, so **FAISS stands by default**. The capability (**M2-02**) is Milestone 2 |
| **RO-02** — `ALTM-KNOWLEDGE-1` determinability | **Precondition satisfied, rule not activated.** RO-01 supplied the canonical designation the rule needed, but activating it is a diagnosis-layer change P3.7.5 did not make and this sprint may not make |
| **RO-05** — `datasets/synthetic/` purpose decision | **Open.** `docs/architecture.md` §11 requires it be decided before corpus data is written there; Milestone 1B's corpus expansion is the first work with a reason to |
| **1B-11** — `documents[].indexed` semantics | **Allocated to Milestone 1B, unresolved by design.** Verified `false` on both entries at §4 |
| All remaining Milestone 1B / 2 / 3 capabilities | Allocated, not built. See `docs/DEFERRED_ITEMS_REGISTER.md` |
| **P3.7.5 F-2** — `manifest_version` not incremented | **Open Repository Owner decision.** See §7.1 |

**No incomplete authorization was discharged.**

---

## 7. Accepted Limitations

Consolidated. **No new deferred work is introduced, and no existing allocation is changed.**

| # | Limitation | Authority |
|---|---|---|
| **L-1** | **`documents[].indexed` semantics deferred to Milestone 1B.** The field is `false` on both entries while both documents are demonstrably chunked. It is **undefined**, not wrong: its contract names an indexing stage the repository does not have | **1B-11**; `docs/P3.7.3_…` §4.1 R-1B-11 |
| **L-2** | **Retrieval Infrastructure deferred.** No `Indexer`, no `EmbeddingProvider`, no `VectorStore`, no placeholder vectors, no index-coverage validation (DQ-7), no corpus beyond two resume versions | **1B-01 … 1B-10**; `docs/P3.7.3_…` Decision 5 |
| **L-3** | **AI enablement deferred.** No embeddings, vector search, BM25, hybrid retrieval, reranking, LLM generation, Ragas or DeepEval. Criterion **A-5** holds and is structurally enforced by AST allowlist specifications | **M2-01 … M2-17**; `docs/MILESTONE_1A.md` Out of Scope |
| **L-4** | **Cross-version synthesis intentionally excluded.** Multi-document synthesis and contradiction resolution between the two resume versions are out of scope. Two of 22 Evidence Trace entries (`f026_reasoning_01`, `f026_reasoning_02`) are version-comparison questions expecting superseded-document chunks; canonical preference makes the second unanswerable | `docs/GENERATION_CONTRACT.md` §21; `docs/P3.7.5_…` §5.4, finding **P3.7.5 F-3** |
| **L-5** | **Canonical designation is an ordering mechanism rather than an exclusion mechanism.** Duplicate wording remains eligible for retrieval where the canonical document supplies fewer than `top_k` tying chunks; it is ordered after the canonical wording rather than removed | `docs/P3.7.5_…` §6.3, §9.1, finding **P3.7.5 F-1** |
| **L-6** | **No deduplication exists.** Two chunks with identical or near-identical text from different documents are distinct candidates | `docs/P3.7.5_…` §9.5 |
| **L-7** | **Canonical designation is corpus-specific.** `CANONICAL_SOURCES` names one path; an undesignated document class defaults to non-canonical. Milestone 1B's corpus expansion will need its own designation | `docs/P3.7.5_…` §9.3 |
| **L-8** | Accepted limitations of `docs/P3.5.2_…` §9 and `docs/P3.6.0_…` §9 — whole-chunk evidence spans, corpus reload per invocation, abstention reachable only by lexical disjointness, `--question` as the entire interface, POSIX-only byte-identity, and others | **NA-04**; recorded as accepted, not as defects |

### 7.1 One limitation the brief did not name, carried forward rather than dropped

**P3.7.5 finding F-2 — `manifest_version` remains `1.0` after a schema extension.** `docs/MILESTONE_1A.md` build item 1 states that *"`manifest_version` changes only when the Knowledge Manifest schema itself changes"*, and adding `canonical` is a schema change. It was not incremented because P3.7.5's own brief required preserving the existing schema, `validate_manifest` rejects any other value, and the contract freezing `1.0` is a governance document that sprint could not modify.

Work Package 5's enumeration did not include it. It is recorded here because a finding raised at P3.7.5 §8 and left unresolved would otherwise lapse silently at closure, and **CP-8** requires the gap be recorded rather than the statement softened. **This records an existing finding; it introduces no new work and reallocates nothing.** Resolution is a Repository Owner decision, available at any time and blocked by nothing.

---

## 8. Milestone Transition Summary

```text
Milestone 1A — Deterministic Knowledge Pipeline        ✅ CLOSED (this sprint)
        │
        ▼
Milestone 1A Capability Inventory                      ◀ next
        │
        ▼
Career Asset Synchronization
        │
        ▼
Milestone 1B — Retrieval Infrastructure Foundation
```

**What changes at the 1A → 1B transition:** the Index Layer is built; `EmbeddingProvider` and `VectorStore` are defined; the corpus grows beyond two resume versions; DQ-5, DQ-6 and DQ-7 close the Data Quality Validation set; `documents[].indexed` acquires a determinable meaning.

**What does not change:** determinism. Criterion **A-5 remains binding throughout Milestone 1B** and its AST allowlist specifications remain in force. No model, vector database, BM25 implementation, LLM or evaluation tool enters the repository at this transition.

**Milestone 1B restores** `docs/roadmap.md` §3's Locked Implementation Order — *Chunking → Indexing → Retrieval* — which the repository executed out of order when Retrieval shipped at P3.3.1 while Indexing was never built.

**Two Repository Owner decisions are open before Milestone 1B writes corpus data:** **RO-05** (`datasets/synthetic/` purpose) and canonical designation for the new document classes (**L-7**). Neither blocks closure; both precede 1B's corpus work.

Full transition detail: `docs/P3.7.3_…` Work Package 6. Capability set: `docs/DEFERRED_ITEMS_REGISTER.md` §3.

---

## 9. Sprint compliance

| Constraint | Status |
|---|---|
| Implementation modified | **No** |
| Retriever / Generator modified | **No** |
| Manifest modified | **No.** Verified by in-memory rebuild; nothing written |
| Datasets modified | **No.** Three digests unchanged |
| Governance modified | **No** |
| Constitutional decisions reinterpreted | **No.** §5.1 reproduces the Scope Ruling; it does not re-judge it |
| Historical reports modified | **No.** Byte-identical |
| `EmbeddingProvider` / Index Layer / embeddings / vector search introduced | **No** |
| `indexed` resolved | **No.** Verified unchanged |
| Milestone 1B or 2 begun | **No** |
| New acceptance criteria introduced | **No** |
| New deferred work introduced, or existing work reallocated | **No** |
| Executable specifications | **372 / 372 passing**, none modified |
| Git operations performed | **No.** All changes left unstaged |
| Files added | **One** — this report |

---

## 10. Closure

> ### Milestone 1A is formally accepted and its deterministic engineering baseline is frozen at commit `fd6c4c7`.
>
> The repository is **complete for Milestone 1A** and **ready for the Milestone 1A Capability Inventory.**

Milestone 1A established the repository's deterministic engineering foundation. Future milestones **SHALL extend this foundation while preserving its traceability, reproducibility and governance guarantees.**

The property this milestone leaves behind is not the pipeline. It is that **the repository's own documentation accurately describes its own behaviour** — which is why a milestone could close here with two Definition of Done items unmet and say so plainly, in the same document that certifies everything it did achieve.

---

*Sprint P3.7.6 complete. All changes left unstaged.*

```text
Repository Owner Acceptance

Repository Owner: anthropologenie

Milestone 1A Closure & Frozen Baseline

ACCEPTED

This document records the Repository Owner's formal acceptance of the
Milestone 1A Closure & Frozen Baseline.

Repository Owner acceptance is complete.

No further repository modification is authorized within Milestone 1A
except the manual Git commit performed by the Repository Owner.
```

---

# Erratum E-2 — Milestone 1B Corpus Synchronization

**Appended, not edited.** Under `docs/P3.7.3_Repository_Owner_Constitutional_Decision.md` **CP-3** — *"A committed report is an accurate statement of repository state at the commit that produced it. It is never edited, even when later found inaccurate. Corrections are recorded forward."* Nothing above this line was altered. This erratum follows the mechanism established by Contract Erratum **E-1** (`docs/DOCUMENT_CONTRACT.md` §8.9).

## E-2.1 What remains accurate

**§3.1 and §4 above remain the accurate record of commit `fd6c4c7` and are NOT corrected.** Every digest, count, and designation they record was true when measured. This erratum records supersession by later intentional change; it does not report an error.

Specifically unchanged and still true:

- **372 / 372 passing** — the specification count is the same before and after this synchronization.
- The three `datasets/golden/*` digests — `d5035f4013fc`, `c8c8f120f423`, `f45c2c2f5f41`.
- §3's binding condition: *"Subsequent milestones **SHALL extend** this baseline. They **SHALL NOT redefine** it."* See §E-2.5.

## E-2.2 The corpus was intentionally extended after closure

Commit **`8ddcaa7`** — *docs(corpus): add canonical Milestone 1A resume corpus* — added `Karthik_SR_Resume_v3_0.docx` and `documents/resume/README.md` beneath `sample_rag/documents/resume/`. This was an intentional evolution of the human knowledge corpus by the Repository Owner. It left the machine-readable state unsynchronized, which **DQ-3** detected exactly as designed — a corpus file present with an approved extension and no `documents[]` entry.

## E-2.3 Three Repository Owner decisions executed

Executed together as Milestone 1B's first engineering activity:

| Decision | Outcome |
|---|---|
| **RO-01** — Canonical Document Marking | **Resume v3.0 is canonical.** v2.2 and v2.3 remain historical corpus artifacts |
| **Documentation relocation** | `sample_rag/documents/resume/README.md` → `docs/corpus/resume-corpus.md`. It leaves `SUPPORTED_EXTENSIONS` discovery scope: no longer catalogued, chunked, or retrievable |
| **RO-05** — `datasets/synthetic/` | **Removed.** Any future synthetic dataset requires an explicit Repository Owner decision naming its architectural purpose and consuming milestone |

**RO-02** was discharged as a consequence of RO-01, on its own recorded terms — *"Follows RO-01 automatically; requires no engineering."* No engineering was performed for it.

## E-2.4 Superseded digests

Two of the five §3.1 digests are superseded. The other three are unchanged.

| Authority | §3.1 value (at `fd6c4c7`) | Value after this synchronization |
|---|---|---|
| `sample_rag/knowledge_manifest.json` | `584369393c7d` | **`84a3c4e7a853`** |
| `sample_rag/chunks.json` | `323723b4fe82` | **`9cd47383f79d`** |
| `datasets/golden/resume_facts.json` | `d5035f4013fc` | `d5035f4013fc` — unchanged |
| `datasets/golden/resume_qa_pairs.json` | `c8c8f120f423` | `c8c8f120f423` — unchanged |
| `datasets/golden/resume_evidence_trace.json` | `f45c2c2f5f41` | `f45c2c2f5f41` — unchanged |

Corresponding §4 statements superseded: *"Deterministic manifest digest … (`584369393c7d`)"*; *"Canonical document designation ✅ `bac26d34f834` (v2.3) `canonical: true`"*; *"`documents[].indexed` ✅ `false` on **both** entries"* — now three entries, all `false`, per **1B-11**, which remains unresolved; and *"Reproducibility — CLI answer path (`b02e98aab0de…`)"*, whose value changes because v3.0 is now present and canonical. The CLI abstain path is unchanged, and byte-identity across runs is preserved on both paths.

## E-2.5 Canonical designation moved

| | Before | After |
|---|---|---|
| `3f3797c1134c` — v2.2 | `canonical: false` | `canonical: false` |
| `bac26d34f834` — v2.3 | **`canonical: true`** | **`canonical: false`** |
| `d1a4d530f9c1` — v3.0 | *(not catalogued)* | **`canonical: true`** |

**v2.2 and v2.3 remain historical corpus artifacts** — retained on disk, catalogued in the Manifest, and chunked (86 chunks each). Supersession is expressed by the flag, never by removal. The corpus is 3 documents and 259 chunks.

**§3's extension condition is satisfied.** Two of the three entries are byte-identical to their frozen form; the third is an addition. The single in-place change — v2.3's `canonical` flag — is the substance of RO-01 itself, not a redefinition of the baseline's contents.

## E-2.6 `docs/P3.7.5_…` is superseded and remains accurate

`docs/P3.7.5_Canonical_Document_Marking_Report.md` designated **v2.3** canonical and recorded the manifest digest transition ending at `584369393c7d`. **That report remains the accurate record of its own commit and is not corrected.** Its designation is superseded by RO-01, which was taken against a corpus that did not exist when P3.7.5 was written: v3.0 entered the repository afterward, at `8ddcaa7`.

Per the Repository Owner's direction, this supersession is recorded **here**, in this single erratum. **No separate erratum is written for `docs/P3.7.5_…`, and that report is not edited.**

## E-2.7 Verification at this synchronization

| Verification | Result |
|---|---|
| Executable specifications | ✅ **372 / 372 passing** |
| **DQ-3** completeness | ✅ **PASS** — the check that detected the unsynchronized state |
| Deterministic manifest digest | ✅ In-memory regeneration **byte-identical** to the committed artifact (`84a3c4e7a853`) |
| Chunk collection | ✅ 259 chunks over 3 documents; `validate_chunks(load_chunks())` passes |
| Evaluation | ✅ determinism, stable ordering, referential integrity, exhaustive classification, classification totals, repository integrity — **all PASS** |
| Diagnosis (ALTM) | ✅ **11 / 11 PASS**, including *unreachable stages absent* and *document identity integrity* |
| Metrics | ✅ **10 / 10 PASS**, including deterministic computation and reproducibility |
| Criterion **A-5** | ✅ Unaffected — no embedding, vector-store, or LLM-evaluation import added |
| Contracts | ✅ `docs/DOCUMENT_CONTRACT.md`, `docs/CHUNK_CONTRACT.md`, `docs/GENERATION_CONTRACT.md` unchanged |

**Recorded, not corrected:** `datasets/golden/resume_facts.json` anchors 25 of 26 facts to `bac26d34f834` (v2.3) and 1 to `3f3797c1134c` (v2.2); **none to v3.0**. Retrieval against the Golden Dataset moves accordingly — full coverage 10 → 8, partial 5 → 3, no match 7 → 11. This is the arithmetic consequence of designating a canonical document the Golden Dataset does not yet cover. No register capability currently owns re-deriving the resume Golden Dataset against v3.0. Recorded here as an engineering fact for the Repository Owner; **no action is taken or implied by this erratum.**

---

# Erratum E-3 — Sprint 1B.2A Evaluation Synchronization

**Appended, not edited.** Same mechanism and same authority as **E-2**: `docs/P3.7.3_Repository_Owner_Constitutional_Decision.md` **CP-3**. Nothing above this line was altered, including **E-2** itself.

## E-3.1 What this erratum records

**E-2.7's closing paragraph is discharged.** It recorded, as an engineering fact and without taking action, that `datasets/golden/resume_facts.json` anchored 25 of 26 facts to `bac26d34f834` (v2.3) and **none to v3.0**, and that retrieval classification moved *"full coverage 10 → 8, partial 5 → 3, no match 7 → 11"* as the arithmetic consequence. Sprint 1B.2A re-derives the resume Golden Dataset against the canonical document and reverses that movement exactly.

**E-2.1 is superseded in one respect and otherwise stands.** It recorded the three `datasets/golden/*` digests as unchanged by the corpus synchronization. That was true at that commit. All three change here — see §E-3.4.

## E-3.2 Facts re-anchored, facts regenerated

The 25 facts parented by v2.3 were separated by whether their **grounding text** — the verbatim `source_text` quotation, not the repository's restatement — differs between v2.3 and v3.0. No fact was regenerated whose grounding text was unchanged.

| Disposition | Count | Facts |
|---|---|---|
| **Re-anchored only** — `document_id` / `document_source` re-pointed to `d1a4d530f9c1`; `source_text`, `fact` and `section` byte-identical | **17** | `f002`, `f004`, `f005`, `f006`, `f008`, `f009`, `f011`, `f012`, `f015`, `f016`, `f017`, `f018`, `f019`, `f020`, `f021`, `f022`, `f025` |
| **Regenerated** — grounding text genuinely differs in v3.0 | **8** | `f001`, `f003`, `f007`, `f010`, `f013`, `f014`, `f023`, `f024` |
| **Untouched** — parented by v2.2, the Stale Version / Contradiction evidence | **1** | `f026` |

Of the 8 regenerated, only **2** carry a changed claim: `f001` (the professional summary was rewritten from *"QA Lead with around 7.5 years of backend-focused engineering experience…"* to *"Quality Engineering leader with around 7.5 years progressing… into AI Quality Engineering"*) and `f007` (v3.0 replaces the single `AI Engineering` skills line with two — `AI Evaluation & Quality Engineering` and `AI Frameworks & Protocols`). The remaining 6 differ only in the quotation: one preposition (`f003`), ASCII arrows becoming `→` (`f010`), `'` becoming `’` (`f013`, `f014`), and a dropped sentence-final period (`f023`, `f024`).

Five QA-pair `expected_answer` values were synchronized in consequence — four parented by `f001`, one by `f007`. No question text, identifier, category, or count changed. The Evidence Trace Dataset was regenerated in full by `scripts/build_evidence_trace.py`; it is derived, and no entry was hand-edited.

**Fact granularity was preserved rather than expanded.** v3.0 splits `f007`'s single skills category in two. Authoring a second fact would have grown the identifier space and required new QA pairs — corpus authoring, not synchronization — so `f007` was re-anchored to a quotation spanning both successor lines, keeping its claim, its id, and its dependent QA pair intact.

## E-3.3 The Stale Version / Contradiction pair is deliberately **not** re-anchored

`resume_f026` and its two QA pairs — `qa_resume_f026_reasoning_01` (**Stale Version**) and `qa_resume_f026_reasoning_02` (**Contradiction**) — are unchanged, and their two Evidence Trace entries are byte-identical to their pre-synchronization form.

These two categories structurally require **two disagreeing versions**, so the promotion of v3.0 forks how they could be anchored, and the fork produces materially different test cases. That choice is reserved to the Repository Owner and is **the one item this sprint stops on**. Both expected answers currently name v2.3 as *"the current canonical resume"*, which the Manifest no longer supports; the statement is left standing rather than repaired, because repairing it would decide the fork.

## E-3.4 Superseded digests

| Authority | E-2.4 value | Value after this synchronization |
|---|---|---|
| `sample_rag/knowledge_manifest.json` | `84a3c4e7a853` | `84a3c4e7a853` — unchanged |
| `sample_rag/chunks.json` | `9cd47383f79d` | `9cd47383f79d` — unchanged |
| `datasets/golden/resume_facts.json` | `d5035f4013fc` | **`30b71471c871`** |
| `datasets/golden/resume_qa_pairs.json` | `c8c8f120f423` | **`61a8bdb830f8`** |
| `datasets/golden/resume_evidence_trace.json` | `f45c2c2f5f41` | **`30d25e59b32f`** |

The Knowledge and Index authorities are byte-identical: `scripts/build_manifest.py` and `scripts/build_chunks.py` were re-run and produced no change. **§3's extension condition holds** — this synchronization moves the evaluation authorities onto the corpus the baseline already contains; it redefines nothing in the corpus itself.

## E-3.5 Retrieval restoration

E-2.7's recorded movement is reversed exactly, and every gain is a Knowledge-side consequence — no retrieval, ranking, chunking, indexing, or generation code was touched.

| Measure | At E-2.7 | After synchronization |
|---|---|---|
| Full Coverage | 8 | **10** |
| Partial Match | 3 | **5** |
| No Match | 11 | **7** |
| Hit Rate | 0.5 | **0.6818** |
| Chunk Precision@K (macro) | 0.1 | **0.1364** |
| Chunk Recall@K (macro) | 0.4205 | **0.5318** |
| `ALTM-RETRIEVE-1` — *right topic, wrong specific document* | 3 | **0** |

`ALTM-RETRIEVE-1` returning to **0** is the substantive result: the three questions expecting a document the retriever no longer prefers were expectation drift, not a retrieval regression. The P3.7.5 canonical tie-break remains correct and untouched; what had drifted was what the Golden Dataset expected of it.

## E-3.6 One specification added

**X-17** (`tests/test_cross_dataset_integrity.py`) — a QA pair outside the Stale Version and Contradiction categories may not cite a fact anchored to a non-canonical document. This is the gate whose absence let the drift E-2.7 recorded stay invisible to a fully green suite: every prior specification resolves a reference to whatever document a fact names, and none asks whether the Manifest still designates that document canonical.

It reads canonicality from `sample_rag/knowledge_manifest.json` rather than from any filename, version string, or literal id, so it generalizes to the next corpus evolution — job descriptions and JobOps data included — without maintenance. Verified against the pre-synchronization dataset, where it fails and names all 32 drifted citations.

## E-3.7 Verification at this synchronization

| Verification | Result |
|---|---|
| Executable specifications | ✅ **418 / 418 passing** (417 + X-17) |
| Evidence Trace regeneration | ✅ Idempotent — re-running the builder reproduces the artifact byte-identically |
| Manifest / Chunk Corpus | ✅ Rebuilt in place, **no change** |
| Evaluation | ✅ determinism, stable ordering, referential integrity, exhaustive classification, classification totals, repository integrity — **all PASS** |
| Metrics | ✅ **10 / 10 PASS** |
| Diagnosis (ALTM) | ✅ **11 / 11 PASS**, including *document identity integrity* |
| CLI runtime behaviour | ✅ Answer and abstain paths **byte-identical** to their pre-synchronization output, and reproducible across runs |
| Criterion **A-5** | ✅ Unaffected — no embedding, vector-store, or LLM-evaluation import added |
| Contracts | ✅ `docs/DOCUMENT_CONTRACT.md`, `docs/CHUNK_CONTRACT.md`, `docs/GENERATION_CONTRACT.md` unchanged |
| `docs/roadmap.md`, `docs/architecture.md`, milestone allocation | ✅ Unchanged |

**Recorded, not acted on:** no `docs/DEFERRED_ITEMS_REGISTER.md` capability owns the work this sprint performed — E-2.7 recorded that, and it remains true. Nor does one own the Stale Version / Contradiction re-anchoring decision §E-3.3 stops on. Creating a register row is an allocation, and §1.3 of that register reserves allocation to a Repository Owner decision; **this sprint therefore creates none.**

## E-3.8 Blocked items — synchronization requiring Repository Owner authority

Two artifacts carry statements this synchronization falsifies, and neither may be edited by an implementation sprint. Both are recorded forward here instead.

| Artifact | Statement now inaccurate | Why not edited |
|---|---|---|
| `docs/MILESTONE_1A.md` build items **7** and **8** | *"Digests `d5035f4013fc`, `c8c8f120f423`"* and *"Digest `f45c2c2f5f41`"* | **Locked** repository authority. Its Milestone Synchronization Record is amendable only under `docs/P3.7.3_…` authorization **A1**, which an implementation sprint does not hold. §E-3.4 above is the forward record |
| `docs/DEFERRED_ITEMS_REGISTER.md` | No row owns this work, or the §E-3.3 decision | Adding a row is a milestone allocation — §1.3 reserves it to a Repository Owner decision |

**Deliberately not swept, and why.** Every other occurrence of the superseded digests and the `372` specification count sits in a committed P3.x report, where **CP-3** forbids editing and this erratum is the prescribed remedy. `docs/ENGINEERING_TRACEABILITY_REGISTER.md` §7 is scoped *"at commit `d9a6db4`"* and last synchronized at P3.7.2 — accurate as written, and not made inaccurate here. `README.md`, `sample_rag/README.md`, `datasets/README.md`, `datasets/SCHEMA.md`, `docs/corpus/resume-corpus.md` and `docs/DATA_QUALITY_VALIDATION_PLAN.md` were each checked and carry no statement this sprint falsifies.

`docs/PROJECT_ORIENTATION.md` and `docs/MILESTONE_1A_CAPABILITY_INVENTORY.md` carry counts (`372` specifications, `172` chunks, *"two versions — v2.2 historical, v2.3 canonical"*) already superseded by Sprints 1B.1 and 1B.2 and by the corpus synchronization — **before** this sprint, and left unsynchronized by each of them. Whether they are living documents or Milestone 1A records is unresolved, and resolving it is not an implementation decision. Reported, not edited.
