# Engineering Traceability Register

**Repository:** `ai-quality-engineering`
**Status:** Active — established at Sprint P3.1.7.2 (Assurance Remediation)
**Scope at establishment:** Knowledge Layer (Milestone 1A). Later layers are added as their decisions are dispositioned.
**Last synchronized:** Sprint P3.7.2 (Repository Governance Synchronization) — Retrieval, Evaluation, Dataset, Generation and CLI layers added, per `docs/P3.7.2_Repository_Governance_Synchronization_Report.md`; then Sprint **RO-09**, which added one open observation (**M2.01B-F-1**, §3.5) and changed nothing else

---

## 1. Purpose

A durable, **retrospective** record of engineering dispositions that outlives the sprint that produced them.

The repository reached Sprint P3.1.7 holding its engineering record in artifacts never intended to carry it: deferred findings in a pytest `conftest.py` docstring, resolved design decisions in module docstrings, and sprint provenance in commit messages alone. Both independent reviews at Sprint P3.1.7 recommended this register (`P3.1.7-GOV-01`, Major; `G-1`/`G-2`/`G-3`/`G-4`), and Sprint P3.1.7.1 verified every one of those governance findings against repository evidence.

### 1.1 Responsibilities — limited to five

1. Deferred findings
2. ADR references
3. Cross-sprint implementation observations
4. Review dispositions
5. Claim-to-specification mappings

### 1.2 What this register is NOT

Explicitly verified at establishment, and a standing constraint on every future edit:

| Not a… | Because |
|---|---|
| **Roadmap** | It contains no milestone sequence, no dates, no planned direction. `docs/roadmap.md` owns that. |
| **Backlog** | It contains no inventory of intended work. A deferred *finding* records a decision already taken about where a known issue belongs — it is not a work item awaiting scheduling. |
| **Sprint tracker** | It records no sprint status, progress, or completion state. |
| **Task manager** | It contains no tasks, assignees, priorities, or states. |

**The governing test:** every row is retrospective — a decision made, a finding dispositioned, a claim mapped. **If a row describes work to be scheduled, it does not belong here**; it belongs in `docs/roadmap.md` or a sprint brief. Entries are added only *after* a disposition has been established, never in anticipation of one.

---

## 2. Accepted architectural decisions

| ADR | Question | Decision | Sprint | Status |
|---|---|---|---|---|
| [`ADR-0001`](adr/ADR-0001-chunk-persistent-representation.md) | Does Chunk need a separate persistent-representation contract? | No — resolve the container shape inside Serialization Planning | P2.3.0 | Accepted |
| [`ADR-P3.1.7.2-F2`](adr/ADR-P3.1.7.2-F2-corpus-root-containment.md) | Where is corpus-root containment enforced — Construction or Data Quality Validation? | **Option A — Construction** | P3.1.7.2 | **Accepted** (repository owner) |

**Dispositions recorded outside the ADR series.** Two Generation gaps were dispositioned by the Repository Owner in the contract itself rather than by a separate ADR file, and are recorded here so the register remains the single index of accepted decisions. Neither creates an ADR; both cite the frozen authority that carries them.

| Reference | Question | Decision | Sprint | Status |
|---|---|---|---|---|
| `docs/GENERATION_CONTRACT.md` §22, **G-1** | The Generation artifact name | `GenerationResult` approved as the Generation artifact | P3.5.1-G | **Approved** (Repository Owner) |
| `docs/GENERATION_CONTRACT.md` §22, **G-2** | The Milestone 1A runtime interface | `Generator.generate(query, retrieval: RetrievalResult)` approved, in place of `generate(prompt)` | P3.5.1-G | **Approved** (Repository Owner) |
| `docs/DEFERRED_ITEMS_REGISTER.md` §4.5, **RO-13** D-1 | Can model-backed generation proceed under frozen Generation Contract v1.0.0? | **No.** **Generation Contract v2.0.0** authorized for model-backed Milestone 2, recorded adjacently at `docs/GENERATION_CONTRACT.md` §24; **v1.0.0 (§1–§23) preserved byte-for-byte** as the historical Milestone 1A contract | RO-13 | **Approved** (Repository Owner) |
| **RO-13** D-2 — **G-14** | May the Generator perform network I/O? | **Exactly one sanctioned provider interaction** at the generation boundary. Filesystem I/O stays barred; no general filesystem, corpus, retrieval, indexing, arbitrary-network, tool, memory or model-routing access | RO-13 | **Approved** (Repository Owner) |
| **RO-13** D-2 — **G-9** | Does determinism survive a model? | **Split.** Structural determinism (prompt/provenance, request construction and shape, response parsing, schema mapping, error classification, all non-answer fields) **SHALL** hold; **model-output reproducibility of `answer_text` is NOT guaranteed**, and request reproducibility SHALL NOT be reported as it | RO-13 | **Approved** (Repository Owner) |
| **RO-13** D-2 — **G-7** | Does support-by-construction survive synthesis? | **Narrowed, not deleted.** `answer_text` MAY be synthesized; **`SupportingEvidence` SHALL remain grounded in the assembled `Prompt` context and provenance**, with no independent retrieval or corpus path. Empirical Faithfulness / Groundedness remain later evaluation work and are **not claimed** | RO-13 | **Approved** (Repository Owner) |
| **RO-13** D-2 — **G-13 / §18** | What may the v2 Generator consume? | A **`Prompt`**, plus G-14's single provider interaction. `RetrievalResult` is **not** a v2 input; **§18's barred list carries forward unchanged and in full** | RO-13 | **Approved** (Repository Owner) |
| **RO-13** D-3 — **U-1** | How does the Generator consume `Prompt`? | **`Generator.generate(prompt: Prompt) -> GenerationResult`** — `docs/architecture.md` §5's original signature and §6.2's *"Milestone 2 target, reached when a Context Builder exists"*, whose condition **M2-12** satisfied. Exactly one authoritative generation input path | RO-13 | **Approved** (Repository Owner) |
| **RO-13** D-3 — **U-2** | Is the three-field `Prompt` sufficient? | **No — and the shortfall is evidenced, not speculative.** `SupportingEvidence` §8.3 needs `document_id` and both document-frame offsets, which `chunk_ids` does not carry and G-13 bars fetching. `Prompt` gains an ordered `provenance` of **exactly** `chunk_id`, `document_id`, `character_start`, `character_end` — **no chunk text, no scores, no other field** | RO-13 | **Approved** (Repository Owner) |
| **RO-13** D-4 — **A-5** | Third dependency exception? | **Authorized in category and scope, library NOT selected** — one LLM/provider dependency, one module, M2-06 only. Not yet taken; `requirements.txt` unchanged | RO-13 | **Approved** (Repository Owner) |
| **RO-13** D-5 — **M2-14** | `docs/architecture.md` §5 `Generator` row | **Synchronization authorized, not performed.** **M2-14 is NOT discharged** and remains a separate open capability; no architecture document was edited | RO-13 | **Authorized, unperformed** (Repository Owner) |
| `docs/DEFERRED_ITEMS_REGISTER.md` §4.6, **RO-14** D-1 | Are `Generator` and `ModelGenerator` one architectural component or two? | **Two — the dual-path generation architecture is authorized.** **`Generator`** (`generate(query, retrieval)`, v1.0.0, `scripts/cli.py`) is the **frozen Milestone 1A deterministic / reference** component; **`ModelGenerator`** (`generate(prompt: Prompt)`, v2.0.0, `scripts/run_generation.py`) is the **Milestone 2 model-backed** component. Both are authorized architectural components; they are **not** two implementations of one current interface, and the §2 interface-first principle is **not** claimed to be retroactively satisfied between them. Shared `GenerationResult` / `GeneratedStatement` / `SupportingEvidence` artifact types are authorized and are not a component merger | RO-14 | **Approved** (Repository Owner) |
| **RO-14** D-2 — **P3.7.6** | Does the Milestone 2 component displace the Milestone 1A baseline? | **No. The frozen baseline is preserved.** `scripts/cli.py` remains on `Generator`; `tests/test_generator.py` stays frozen at 48 and `tests/test_cli.py` at 27; §4's byte-identical reproducibility remains valid acceptance evidence; the deterministic path is not retired; **no provider call enters the deterministic suite.** `docs/P3.7.6_…` is not edited | RO-14 | **Approved** (Repository Owner) |
| **RO-14** D-3 — **RO-13 D-5** | Can RO-13 D-5's singular-row target still be applied? | **No — and it is superseded only as to that target.** M2-06 delivered two concrete components, so a single `Generator` row carrying `generate(prompt: Prompt)` would assert a signature the class named `Generator` does not have. **RO-13 Decisions 1–4 remain in force in every part; RO-13 is not withdrawn and is not rewritten**; §24.2 remains the v2.0.0 method contract; **no v2.1.0 or v3.0.0 is created** | RO-14 | **Approved** (Repository Owner) |
| **RO-14** D-4 — **M2-14** | Who records the authorized identity in `docs/architecture.md`? | **M2-14, whose scope is widened to include component identity.** RO-14 authorizes the **minimum** synchronization necessary to represent both components, their contract eras, their execution paths and the preserved M1A CLI path — and **deliberately prescribes no text, layout, row count or diagram change.** **`docs/architecture.md` is NOT modified by the ruling, M2-14 is NOT discharged**, M2-14 remains one capability, and no derivative identifier is created | RO-14 | **Authorized, unperformed** (Repository Owner) |
| **RO-14** D-5 — **M2.06-F-2** | Does §24.3's *"§8's field definitions unchanged"* also freeze §8.1's `answer_text` sentence? | **No — the specific G-7 transition governs.** Under v2.0.0 `answer_text` **MAY be model-synthesized**; §16 is where guarantees are normative and §24.3 names G-7 as transitioning. The **field is structurally unchanged**; §8.1 is **not edited** and remains accurate for v1.0.0; `GeneratedStatement.text` stays a verbatim quotation; `SupportingEvidence` stays grounded in `Prompt` context and provenance; **no faithfulness or groundedness guarantee is created.** Recorded at `docs/GENERATION_CONTRACT.md` **§25.1** | RO-14 | **Approved** (Repository Owner) |
| **RO-14** D-6 — **G-5** | G-5 names `RetrievalResult.chunks`, which the v2 Generator may not consume. Which reading holds? | **The evidence-identity intent is binding; the input citation is historical.** Every `SupportingEvidence` still carries a `chunk_id` resolving to a real committed corpus chunk and a matching `document_id`. Under v2.0.0 this is satisfied through **`Prompt.provenance`** (§24.4); a `RetrievalResult` remains the **upstream source** of that provenance, not a v2 Generator input. **G-6 is unchanged, no evidence is fabricated, no runtime changes, no contract version is created.** Recorded at `docs/GENERATION_CONTRACT.md` **§25.2** | RO-14 | **Approved** (Repository Owner) |
| `docs/DEFERRED_ITEMS_REGISTER.md` §4.7, **RO-15** D-1 | Is a durable execution-evidence capability authorized, and under which identifier? | **Yes — `M2-18`, Execution Evidence / Traceability, is allocated** at Milestone 2, stage **2C** (`docs/roadmap.md` §1.1), class **Deterministic Runtime**. It exists to make a **completed** execution inspectable **after the process exits**. It is **not** an evaluation, optimization, generation or retrieval capability, and **not** production observability infrastructure — **no framework, database, OpenTelemetry integration, collector, dashboard or tracing infrastructure is authorized.** **`M2-16` and `M2-17` are already allocated and are neither reused nor reinterpreted**; `M2-18` is the next available identifier, and **no new milestone taxonomy and no derivative identifier is created** | RO-15 | **Approved** (Repository Owner) |
| **RO-15** D-2 — **M2-07 / M2-08** | Does M2-18 block retrieval or generation evaluation? | **No — `Non-blocking`.** Both obtain their required inputs **in-process**, so **trace absence does not prevent metric computation**; a trace improves the **diagnostic explainability** of a metric, and *"technically necessary to calculate a metric"* **≠** *"valuable for explaining the metric."* **M2-07 and M2-08 remain OPEN, unmodified, undischarged and independently executable**, and **neither becomes a trace-dependent capability**. They **may** be sequenced after M2-18 for diagnostic value; that is preference, not dependency | RO-15 | **Approved** (Repository Owner) |
| **RO-15** D-3 — **`GenerationResult`** | Is a trace record a serialized `GenerationResult`? | **No — it is a distinct cross-stage execution-evidence envelope.** `GenerationResult` is the runtime **generation artifact**; the trace **observes** the pipeline and records selected evidence **about** the execution. It **redefines `GenerationResult` and adds no field to it**, and **changes `Prompt`, `RetrievalResult`, `SupportingEvidence`, `Generator`, `ModelGenerator` and the frozen Milestone 1A path in no part** | RO-15 | **Approved** (Repository Owner) |
| **RO-15** D-4 — **§13.2 / §13.3** | Does the `GenerationResult` serialization form govern the trace envelope? | **No.** **§13.2 / §13.3 govern serialization of `GenerationResult`** and approved persisted artifacts **of that type**; the M2-18 envelope is a **different artifact** and does not become one by containing a projection of generation evidence. **JSONL is AUTHORIZED as the v1 representation** — one execution, one record, appended as executions occur. **Concurrency, locking, rotation, retention, filenames, paths and field ordering are deliberately NOT prescribed** and belong to M2-18. **§13.2 / §13.3 are not amended or edited, and `docs/GENERATION_CONTRACT.md` is byte-for-byte unchanged** — no §26, no erratum, no new contract version | RO-15 | **Approved** (Repository Owner) |
| **RO-15** D-5 — trace content | What may a trace contain, and what may it never contain? | **A capability boundary, NOT a mandatory schema.** **Authorized where available at an existing boundary:** execution identity, timestamp, query, retrieval candidate union, semantic / BM25 / RRF rank, RRF score, source-leg attribution, selected chunk ids, `Prompt` provenance, generation component identity, contract version, provider, model, outcome, `answer_text`, supporting-evidence references, observed latency. **M2-18 MUST verify availability and derive the minimal projection from already-available runtime data; no instrumentation may be invented to manufacture a missing field.** **Excluded:** credentials, bearer tokens, `Authorization` headers, provider secrets, credential-bearing raw requests, raw provider responses, **semantic similarity scores**, duplicated corpus/chunk text, arbitrary `Prompt` duplication, library/version or system telemetry, convenience Git metadata | RO-15 | **Approved** (Repository Owner) |
| **RO-15** D-5 — **`VectorStore`** | May the retrieval contract be widened to expose a similarity score for the trace? | **No. The frozen boundary remains CLOSED.** `docs/architecture.md` §7 and `sample_rag/vector_index.py` expose `query(vector, top_k) -> list[str]` — **ids, not distances** — which is exactly the scope **RO-10** fixed. **No widening of that contract or any equivalent retrieval contract is authorized, and no alternative path to obtain a score may be invented.** **Semantic rank is authorized; semantic score is not** | RO-15 | **Approved** (Repository Owner) |
| **RO-15** D-6 — minimality | Is the complete `GenerationResult` persisted? | **No.** **Persist what cannot otherwise be recovered; reference what can be deterministically recovered** from the committed corpus and provenance. **`answer_text` IS authorized** — it is a genuine execution artifact that **cannot** be reconstructed by rerunning v2 generation, because **RO-13 D-2**'s **G-9** split declines to guarantee model-output reproducibility. **Evidence text and `Prompt` context SHOULD NOT be blindly duplicated** where chunk ids plus the four-field provenance already reference the corpus deterministically | RO-15 | **Approved** (Repository Owner) |
| **RO-15** D-7 — component identity | How is the executing generation component identified in a trace? | **At the observation boundary, by the trace layer.** **RO-14 D-1**'s two components must be distinguishable — preferred minimum conceptual identity **`component`** + **`contract_version`**, which for the current M2-06 path is **`ModelGenerator`** / **`2.0.0`**. **No field is added to `GenerationResult`**, and **`sample_rag/generator.py` and `sample_rag/model_generator.py` are NOT modified to carry trace metadata** | RO-15 | **Approved** (Repository Owner) |
| **RO-15** D-8 — artifact class | Are traces source artifacts? | **No — DERIVED RUNTIME ARTIFACTS**, the class **RO-09** fixed for the FAISS index. **They SHALL NOT be Git-tracked**, and SHALL be excluded via `.gitignore` or an equivalent repository-native mechanism **during M2-18**; **RO-15 prescribes no path and edits no `.gitignore`.** **Credential safety ≠ content sensitivity**: credentials **SHALL NEVER** enter a trace, and separately, query / context / answer / evidence may carry corpus content and are therefore potentially sensitive derived data. **No redaction, anonymization, encryption, database storage, retention policy or external telemetry is introduced** | RO-15 | **Approved** (Repository Owner) |
| **RO-15** D-9 — **ALTM** | Does M2-18 add a pipeline stage? | **No — it is an EVIDENCE LAYER**, sitting across the existing `Retrieve → Assemble → Infer` execution and making already-existing artifacts durable. **`docs/altm.md` is not modified, no ALTM stage is created, `REACHABLE_STAGES` is not widened, U-3 is not resolved or converted into a capability, M3-02 is untouched**, and M2-18 is **not** an orchestration capability | RO-15 | **Approved** (Repository Owner) |
| **RO-15** D-10 — **§10.5** | Does allocating a 52nd capability falsify the historical 51 ↔ P3.7.3 reconciliation? | **No, and the historical statement is preserved unedited.** The **51** capabilities matched `docs/P3.7.3_…` Decision 3 §3.6 **exactly**, and still do; **`docs/P3.7.3_…` did not contain M2-18** and no reader may infer that it did. **M2-18 is a subsequent Repository Owner allocation**, making the **current** count **52**. Recorded through the repository's **adjacent, non-destructive** note mechanism (`docs/DOCUMENT_CONTRACT.md` §8.9 **E-1**, §8.10 **E-2**) at `docs/DEFERRED_ITEMS_REGISTER.md` §10.5; **no historical wording is altered** | RO-15 | **Approved** (Repository Owner) |
| **RO-15** D-11 — **M2-06 / M3-01** | Does M2-18 reopen M2-06 or create an M3-01 dependency? | **Neither.** **M2-06 remains ✅ DISCHARGED** — not reopened, its evidence not modified, its acceptance not altered, and not reinterpreted as incomplete. **M2.06-F-1 and M2.06-F-3 remain open and non-blocking; M2-18 resolves neither** — it may make their behaviour **more observable**, and **observability is not repair.** The observation that **M3-01** could eventually benefit from persisted evidence (because **G-9** does not guarantee v2.0.0 answer reproducibility) is recorded as **future architectural context only**: **M3-01 is not modified, M2-18 is not blocking for it, and no capability is created from it** | RO-15 | **Approved** (Repository Owner) |
| **RO-15** — implementation | Is M2-18 implemented or discharged by this ruling? | **No. `M2-18` is allocated OPEN and is NOT discharged by the ruling that allocates it.** **RO-15 freezes no schema** — no JSON structure, field set, field ordering, key naming, module placement, concurrency semantics, retention policy or filename convention is dictated. `sample_rag/`, `scripts/`, `tests/`, `requirements.txt` and `.gitignore` are **untouched**; **no trace module, directory or instrumentation exists**; **no dependency was added, no provider call was made, no credential was read, and no network access occurred**; **no metric was computed**; and **Ragas, DeepEval and Promptfoo remain unactivated.** **RO-15 fixes WHAT is authorized; M2-18 determines HOW** | RO-15 | **Authorized, unimplemented** (Repository Owner) |

| `docs/DEFERRED_ITEMS_REGISTER.md` §4.8, **RO-16** D-1 | May **M2-07** import Ragas under criterion **A-5**? | **Yes — a fourth A-5 exception is authorized, and it is narrow.** **Ragas**, confined to **`evaluation/ragas/`**, **for M2-07 only**. **A-5 is not lapsed generally**: no other LLM-evaluation library, evaluation framework, model router, agent framework, second or general-purpose SDK, orchestration library or arbitrary HTTP access is authorized, and **M2-08**, **M3-01**, **M2-10** and **M3-06** are not authorized by implication. **RO-13's third exception is neither read, reopened, widened nor taken** — it is scoped to *"M2-06 model-backed generation only"* and excludes *"evaluation frameworks"* by name, which is why a fourth was required. **The exception is AUTHORIZED and UNTAKEN**: no package installed, `requirements.txt` byte-identical, **no `import ragas` anywhere**, so the *"only two A-5 exceptions"* sentence in §4 stands unamended and literally true | RO-16 | **Authorized, untaken** (Repository Owner) |
| **RO-16** D-2 — Ragas version/API | Does RO-16 fix the Ragas version or API? | **No — that is M2-07's, and RO-16 selects nothing inside the boundary it authorizes.** Version, API surface, metric-invocation method, dataset/evaluation adapter, artifact-to-Ragas input mapping, the internal structure of `evaluation/ragas/`, and test structure are all **deliberately unprescribed**. **M2-07 MUST inspect the compatible version against this repository's actual environment and record the selection in implementation evidence** — the pattern **RO-13 D-4** used for the provider library and **RO-08 D-1** for the fingerprint algorithm. **No package is installed by the ruling** | RO-16 | **Approved** (Repository Owner) |
| **RO-16** D-3 — evaluation judge | Ragas metrics need an LLM judge. Which provider is authorized? | **The existing `sample_rag/deepseek.py` boundary — independently authorized, and conditionally.** This is **not** an extension or reinterpretation of **RO-13 D-4**, which is scoped to M2-06 generation and is untouched. **The authorization holds if and only if the selected Ragas API can reuse the existing boundary without** modifying the provider architecture, widening it, adding another provider integration or SDK, adding a model router, or adding arbitrary HTTP access — **otherwise M2-07 STOPs** and reports the incompatibility and the minimum further decision required. **M2-07 is not required to force the fit**, and **rewriting the provider client is not authorized.** **Not authorized:** DeepSeek for arbitrary evaluation frameworks, for **M2-08** automatically, any other provider, any provider SDK, a model router, or general evaluation infrastructure. **RO-14 D-2 is carried forward binding — a judge call is a provider call, and none may enter the deterministic pytest suite** | RO-16 | **Approved, conditional** (Repository Owner) |
| **RO-16** D-4 — separation | Does authorizing Ragas authorize whatever model Ragas needs? | **No, and the ruling states so expressly.** The A-5 library exception (D-1) and the provider-boundary authorization (D-3) are **two separate decisions**, each on its own terms; **neither supplies the other's subject.** Only the explicitly named **Ragas** library and the explicitly named **existing DeepSeek boundary** are authorized | RO-16 | **Approved** (Repository Owner) |
| **RO-16** D-5 — transitive dependencies | Are Ragas' supporting/transitive dependencies authorized? | **No — a dependency does not become authorized by being required.** Additional evaluation libraries, new embedding libraries, further provider SDKs, orchestration libraries, model routers, arbitrary HTTP clients and new vector-store packages are **all outside RO-16**. **M2-07 MUST inspect its actual requirements**, and where one falls outside existing authority it **STOPs** — it does not install it, does not add it to `requirements.txt`, and does not read RO-16 as covering it — reporting the exact dependency and the minimum further decision required | RO-16 | **Approved** (Repository Owner) |
| **RO-16** D-6 — embedding boundary | Does a Ragas metric that uses embeddings authorize an embedding dependency? | **No. RO-16 grants NO new embedding and NO new vector-store exception.** `sentence-transformers` and `faiss-cpu` are unchanged in scope and module, and the `VectorStore` boundary stays closed as **RO-10** and **RO-15 D-5** left it. **M2-07 MUST first determine whether its selected API actually requires an embedding model** for Context Precision / Recall; if one is genuinely required and unauthorized, **M2-07 STOPs and requests the minimum further authorization** rather than manufacturing an exception — and does not reach the existing `BGEEmbeddingProvider` for evaluation on the strength of this ruling | RO-16 | **Approved** (Repository Owner) |
| **RO-16** D-7 — credential safety | What credential discipline binds an authorized judge call? | **The existing discipline, carried forward intact and unweakened.** The credential **SHALL NEVER** be logged, printed, persisted, written into an evaluation report or an M2-18 trace, placed in an exception message, or present in any persisted raw provider payload. **It is structural today and must stay so**: `sample_rag/deepseek.py` exposes **no credential accessor** and reads `DEEPSEEK_API_KEY` inside `complete` alone. **`sample_rag/deepseek.py` is NOT modified by the ruling**, and if M2-07 finds the client cannot safely support the judge boundary that is a **future STOP**, not an implementation liberty | RO-16 | **Approved** (Repository Owner) |
| **RO-16** D-8 — **M2-07 / M2-10** | Does exercising Context Precision / Recall discharge **M2-10**? | **No.** **M2-07** is Ragas activation (Evaluation Tooling); **M2-10** is the Context Precision / Context Recall metric capability, *"reserved for Ragas"*. RO-16 authorizes M2-07 to exercise Ragas **against** that metric target — which `docs/roadmap.md` §5 already names as Layer 2's pair, so real measurements are in scope and not scope creep — but **M2-07 implementation ≠ M2-10 discharge**. **The rows are not merged, M2-10 is not discharged, and its status is unchanged**; any future synchronization is separately governed. **A sprint that measures is not thereby a sprint that discharges the measurement capability** | RO-16 | **Approved** (Repository Owner) |
| **RO-16** D-9 — **M3-06** | Does RO-16 complete the declaration-to-import capability? | **No — M3-06 remains OPEN.** RO-16 authorizes the Ragas import M2-07 needs and nothing broader; `deepeval` and `promptfoo` remain declarations and `evaluation/deepeval/` and `evaluation/promptfoo/` remain empty scaffolds. **Recorded as a finding, not acted on:** M3-06's quoted *"a declaration is not an import, so A-5 holds as written"* becomes partly overtaken **for `ragas` alone** once M2-07 takes the exception, and stays true of the others. **No wording is edited**; whether an adjacent note is owed is a synchronization question for the sprint that takes it, under §1.3 and **RO-10 D-2** | RO-16 | **Approved** (Repository Owner) |
| **RO-16** D-10 — **M2-18** | Is M2-18 a prerequisite for, or modified by, M2-07? | **Neither. M2-18 remains ✅ DISCHARGED and untouched, and RO-15 is unamended.** M2-07 **MAY** use execution evidence diagnostically — the explainability **RO-15 D-2** describes — but **M2-18 is not a technical prerequisite**, and D-2's finding that M2-07 obtains its inputs in-process stands. **`scripts/execution_trace.py`, `tests/test_execution_trace.py`, `docs/M2.18_Execution_Evidence_Report.md` and RO-15 are not modified**, and **no trace field may be added, no `VectorStore` widened and no semantic similarity score exposed** to suit a metric | RO-16 | **Approved** (Repository Owner) |
| **RO-16** — implementation | Is M2-07 implemented or discharged by this ruling? | **No. RO-16 authorizes the boundary; M2-07 determines the implementation inside it.** **Nothing was installed, imported, executed or measured**: `ragas` is still absent from the environment and still a declaration only, `requirements.txt` is byte-identical, and `evaluation/`, `sample_rag/`, `scripts/`, `tests/` and `.gitignore` are untouched. **No judge call was made, no credential read, no network access occurred.** **It discharges nothing** — **M2-07**, **M2-08**, **M2-10**, **M3-01** and **M3-06** all remain **OPEN** — **allocates nothing** (it adds no register row), and **creates no capability, milestone or derivative identifier** | RO-16 | **Authorized, unimplemented** (Repository Owner) |

| `docs/DEFERRED_ITEMS_REGISTER.md` §4.9, **RO-17** D-1 | RO-16 authorized Ragas, and M2-07 then STOPped at RO-16 D-5. What is M2-07's authorized path now? | **A native, repository-owned computation of Context Precision and Context Recall**, faithful to `docs/AI_Quality_Metrics_Reference.md`'s tool-independent definitions and `docs/altm.md`'s Retrieve-stage framing. **M2-07 remains the same capability under the same identifier** — **no new `M2-xx`, no rename, no new milestone, and milestone (2) and stage (2A) unchanged**; **only the implementation mechanism is rescoped.** **A simplified set-overlap calculation is NOT an authorized substitute** — `docs/P3.3.3_…` §3 already records the `chunk_`-prefixed metrics as **not** proxies for these two. **RO-17 prescribes no algorithm, module, entry point, result shape or test strategy**; `evaluation/retrieval_metrics.py` is cited as existing native precedent, not as a design | RO-17 | **Approved** (Repository Owner) |
| **RO-17** D-2 — Ragas disposition | Can Ragas-the-library be adopted for M2-07? | **No — not under the current repository constitution.** **Every release carrying the required Context Precision / Context Recall API hard-depends on `langchain`, `langchain-core`, `langchain-community`, `langchain-openai` and `openai` as core, non-extra dependencies**; the pre-alpha releases that omit LangChain predate that API and pull `sentence-transformers`/`transformers`/`spacy` instead, which **RO-16 D-6** bars. **The dependency is load-bearing, not incidental**: Ragas' custom-judge extension point is defined in LangChain types and the modules reached by `import ragas` import LangChain unconditionally, so repository code could not build a judge without importing an excluded framework — **RO-16 D-5 category B**. **Stated narrowly: not adopted *under the current constitution*, not defective and not permanently prohibited.** **RO-16 is NOT rewritten** — its fourth A-5 exception stands as issued and is simply **untaken**, as RO-13's third is. **No Ragas installed, no import added, `requirements.txt` byte-identical** | RO-17 | **Approved** (Repository Owner) |
| **RO-17** D-3 — **NA-07** | Is LangChain authorized to make Ragas possible? | **No. NA-07 is preserved unchanged in every part** — *"LangChain / LangGraph; agent orchestration; … **Excluded, not deferred**"*, whose own reasoning is that *"allocating one would convert an exclusion into a plan."* **No authorization is created** for LangChain, LangGraph, `langchain-core`, `langchain-community`, `langchain-openai`, other orchestration frameworks, additional provider SDKs, arbitrary HTTP access, model routers or agent frameworks. **`docs/roadmap.md` §7, `docs/architecture.md` §11 and `docs/MILESTONE_1A.md` are NOT modified**, and the repository remains a **two-taken-exception** repository | RO-17 | **Approved** (Repository Owner) |
| **RO-17** D-4 — evaluation judge | Which authority covers the judge on the native path? | **RO-17 itself, independently — not RO-16 D-3.** That decision was conditional on *"the selected Ragas API"*; with Ragas off the path its condition has no subject and cannot carry the native one. **RO-17 independently authorizes the existing `sample_rag/deepseek.py` boundary for M2-07 native evaluation judging**, conditional on reuse **without** modifying provider architecture, changing the credential model, adding another provider integration or SDK, adding a model router, adding arbitrary HTTP access, or otherwise widening the boundary — **otherwise M2-07 STOPs.** **It does not amend or expand RO-13 D-4.** **`sample_rag/deepseek.py` may not be rewritten and no alternate provider client may be created.** Scope is **M2-07 native evaluation only** — not **M2-08**, **M3-01**, general evaluation, or another provider. **Credential safety carried forward on RO-16 D-7's terms and required to stay structural**, and **RO-14 D-2 remains binding — a judge call is a provider call and none may enter the deterministic suite**; live judging stays on an explicit on-demand entry point | RO-17 | **Approved, conditional** (Repository Owner) |
| **RO-17** D-5 — embedding / vector store | Does the native path authorize an embedding dependency? | **No. RO-17 grants NO new embedding and NO new vector-store exception.** `sentence-transformers` and `faiss-cpu` keep their existing scope and module, and the `VectorStore` boundary stays closed as **RO-10** and **RO-15 D-5** left it. **Not authorized:** another embedding library, another vector-store package, any `VectorStore` change, any widening of the embedding seam, or **use of `BGEEmbeddingProvider` for evaluation** merely because it exists for retrieval. **M2-07 MUST first establish what its native semantics actually require**, and **STOPs** — reporting the conflict — rather than installing anything | RO-17 | **Approved** (Repository Owner) |
| **RO-17** D-6 — metric semantics | Does RO-17 fix the algorithm? | **No — the semantics are derived by M2-07 from the authoritative material** (`docs/AI_Quality_Metrics_Reference.md`; `docs/altm.md` §9, §12; `docs/P3.3.3_…` §3) and recorded in implementation evidence. **Two distinctions MUST survive:** **Context Recall** — whether retrieved context contains the **independently authored** expected information, the reference being committed golden ground truth and **never the system's own retrieval output**; **Context Precision** — whether retrieved context is **relevant** on the authoritative semantics rather than on an incidental identifier match. **If the definition requires an LLM relevance judgement, the D-4 judge MAY be used** — whether it does is an implementation finding, not a conclusion reached here. **M2-07 MUST preserve enough evidence to explain a result**, and **RO-17 prescribes no evidence schema, report format or trace shape** | RO-17 | **Approved** (Repository Owner) |
| **RO-17** D-7 — **M2-07 / M2-10** | Does the native path discharge **M2-10**? | **No. Both remain OPEN and separate.** M2-07 is the evaluation **implementation and exercise**; **M2-10** is the **metric capability**. **M2-07 may produce real measurements**, and **M2-07 implementation ≠ M2-10 discharge** — the rows are not merged, M2-10 is not discharged and its status is unchanged; any future synchronization is separately governed. **Recorded as a finding:** M2-10's row reserves the metrics *"for Ragas"*, so its **tool attribution** is overtaken while its substance — not implemented at 1A, belongs to Milestone 2 — stands. **No M2-10 wording edited** | RO-17 | **Approved** (Repository Owner) |
| **RO-17** D-8 — **M3-06** | Is M3-06 affected? | **No — M3-06 remains OPEN and its status is not altered.** `evaluation/deepeval/` and `evaluation/promptfoo/` are **not populated** and neither library is imported. **RO-16 D-9 anticipated that M3-06's *"a declaration is not an import"* would be partly overtaken for `ragas` once M2-07 took the exception; M2-07 did not take it**, so the anticipation did not occur and **the sentence remains true of every declaration, `ragas` included**. **No synchronization is owed or invented** | RO-17 | **Approved** (Repository Owner) |
| **RO-17** D-9 — **M2-18** | Is M2-18 affected? | **No. M2-18 remains ✅ DISCHARGED and RO-15 is unamended and not reinterpreted.** `scripts/execution_trace.py`, `scripts/run_generation.py`, `tests/test_execution_trace.py` and `docs/M2.18_Execution_Evidence_Report.md` are **not modified**, **no trace field may be added for evaluation**, no `VectorStore` widened and no semantic similarity score exposed. **M2-07 MAY consume trace evidence diagnostically** — RO-15 D-2's explainability — but **M2-18 is not a prerequisite and may not be redesigned** | RO-17 | **Approved** (Repository Owner) |
| **RO-17** D-10 — implied authority | Does RO-17 authorize anything beyond the native M2-07 path? | **No.** Not **M2-08** / DeepEval, not **M3-01** / Promptfoo, not retrieval, prompt or generation optimization, not **M2-15** or **M2-17**, not additional evaluation frameworks, not additional providers, and no new capability. Each remains governed by its own existing authority or a future ruling | RO-17 | **Approved** (Repository Owner) |
| **RO-17** — implementation | Is M2-07 implemented or discharged by this ruling? | **No. RO-17 rescopes the authorized path; M2-07 determines the implementation inside it.** **Nothing was installed, imported, executed or measured** — `ragas`, `langchain-core` and `openai` are all absent, `requirements.txt` is byte-identical, and `sample_rag/`, `scripts/`, `tests/`, `evaluation/` and `datasets/` are untouched. **No judge call, no credential read, no network access for evaluation.** **It discharges nothing** — **M2-07**, **M2-08**, **M2-10**, **M3-01** and **M3-06** remain **OPEN** — **allocates nothing** (no register row added), and **creates no capability, milestone or identifier** | RO-17 | **Authorized, unimplemented** (Repository Owner) |

| `docs/DEFERRED_ITEMS_REGISTER.md` §4.10, **RO-18** D-1 | The M2-08 framework reconnaissance is complete. What is M2-08's authorized implementation path? | **A native, repository-owned computation of Faithfulness, Groundedness, Hallucination Rate and Answer Relevancy** — the set fixed at exactly four, with **none added and none dropped because a framework lacks it**. **M2-08 remains the same capability under the same identifier** — **no new `M2-xx`, no rename**, and class, blocking status, milestone (2) and ownership unchanged; **only the implementation mechanism is authorized as native.** **Layer numbering recorded, not amended:** this is `docs/roadmap.md` §5 **Layer 3** and `docs/AI_Quality_Metrics_Reference.md` **Layer 4** — the same layer under two schemes, both correct in their own document. **RO-18 prescribes no algorithm, module, entry point, result shape, report schema, test strategy, fixture name, prompt wording, aggregation formula or CLI design**; M2-07's `evaluation/context_metrics.py` is cited as existing native precedent, not as a design. **M2-08 is not discharged** | RO-18 | **Approved** (Repository Owner) |
| **RO-18** D-2 — DeepEval disposition | Can DeepEval be adopted for M2-08? | **No — not under the current repository constitution**, and the first reason is capability, not dependency. **No release in the full 516-version history implements Groundedness**, which `docs/AI_Quality_Metrics_Reference.md` holds at **Primary**. Separately: releases **0.20.21 – 2.7.9** declare `langchain` as a core, non-extra dependency and import it on the metric path (**NA-07**, *"Excluded, not deferred"*); releases **2.5.6 – 4.1.8** declare `openai` as core and import it at module level whether or not a custom judge is supplied (**RO-16 D-5**); and `deepeval` is itself an LLM-evaluation library (**A-5**). **Recorded as the operative reason: DeepEval is not required, because the capability can be implemented natively without its dependency, provider, orchestration or architecture surface.** **Stated narrowly: not adopted *under the current constitution*, not defective and not permanently prohibited.** **No new A-5 exception; NA-07 not modified; dependency constitution not weakened.** Evidence preserved at `docs/M2.08_Evaluation_Framework_Reconnaissance_Report.md`, commit `81b373a` | RO-18 | **Approved** (Repository Owner) |
| **RO-18** D-3 — Promptfoo | Is Promptfoo part of M2-08? | **No, and RO-18 does not authorize it.** Its PyPI distribution is a subprocess wrapper over a Node.js CLI, so adoption would introduce a **Node runtime**, a **second package ecosystem**, a **second evaluation runtime** and a **second credential boundary** — none admitted by any repository authority and none required by M2-08. **No dependency exception is created.** **M3-01 remains a separate future capability and remains OPEN** — not discharged, row not edited, status unchanged | RO-18 | **Approved** (Repository Owner) |
| **RO-18** D-4 — M2-08 judge seam | Which authority covers the judge for native M2-08? | **RO-18 itself, independently and narrowly — not RO-17 D-4**, whose scope is *"M2-07 native evaluation only"* and which is **not amended, widened, reinterpreted or retroactively extended**; **RO-13 D-4 is equally untouched.** **RO-18 authorizes the existing `sample_rag/deepseek.py` boundary as M2-08's judge seam**, in the shape *native M2-08 evaluator → injected judge seam → existing provider boundary*, and **not** as general authorization for DeepSeek in M2-08 tooling, scripts, frameworks, orchestration or routers. **Not authorized:** another provider SDK, another provider, a model router, arbitrary HTTP access, rewriting `sample_rag/deepseek.py`, retry/orchestration architecture, a second provider client, or a generalized LLM abstraction created merely for M2-08. **The provider implementation must remain architecturally unchanged**; if the boundary cannot support the judge without widening, **M2-08 STOPs** and reports the minimum further decision required — **forcing the fit is not authorized.** **Credential safety carried forward intact and required to stay structural: `DEEPSEEK_API_KEY` SHALL NEVER be logged, printed, persisted, exposed in evaluation evidence, included in an M2-08 report, or placed in an exception message** | RO-18 | **Approved, conditional** (Repository Owner) |
| **RO-18** D-5 — **RO-16 D-3** reconciliation | RO-16 D-3 withheld DeepSeek *"for M2-08 automatically"*. How is that disposed of? | **Superseded ONLY as to that specific M2-08 DeepSeek exclusion, by RO-18 D-4.** **RO-16 D-3 was correct and binding when recorded** — issued before the reconnaissance existed and before a native M2-08 path was established — and **is NOT treated as having been wrong, NOT amended generally and NOT rewritten**; §4.8 stands as issued and remains historically accurate. **The basis is the evidence at commit `81b373a`**: the capability should be native; DeepEval provides no Groundedness across the investigated history; forcing the framework in would introduce deliberately excluded boundaries; and the M2-07 judge-reuse pattern is already proven without widening provider architecture. **Every other RO-16 D-3 restriction remains binding**, and RO-18 authorizes **no** arbitrary evaluation framework, other provider, other provider SDK, LangChain, DeepEval, Promptfoo, Ragas, model router, orchestration or arbitrary HTTP access. **Recorded so the register carries no unexplained RO-16 / RO-18 contradiction** | RO-18 | **Approved** (Repository Owner) |
| **RO-18** D-6 — deterministic tests | May a live judge call enter pytest? | **No. RO-14 D-2 is carried forward binding and not relaxed — a judge call is a provider call.** M2-08's deterministic specifications **MUST** exercise the evaluation path through **deterministic injected judge doubles/fakes**, and **live judging MUST remain confined to an explicit, on-demand evaluation entry point** — the pattern `scripts/run_generation.py` established and `scripts/evaluate_context.py` carried into evaluation. **The concrete fake classes, names and internal test structure are M2-08's**, not prescribed here. **The suite must remain deterministic** | RO-18 | **Approved** (Repository Owner) |
| **RO-18** D-7 — dependencies | Does RO-18 authorize any new dependency? | **No — none, by name: DeepEval, Promptfoo, Ragas (for M2-08), LangChain, LangGraph, the OpenAI SDK, any new HTTP SDK, orchestration and model routers are all NOT authorized.** M2-08 uses existing repository seams and a standard-library/native implementation where possible. **`requirements.txt` is not changed by this governance sprint**, and **no transitive dependency becomes authorized merely because it would be useful** — **RO-16 D-5** governs unamended; a genuine conflict is an **M2-08 STOP**, not an install. **Embedding and vector-store boundaries stay closed** — no new exception, `sentence-transformers` and `faiss-cpu` unchanged in scope and module, `VectorStore` closed as **RO-10** and **RO-15 D-5** left it, and **`BGEEmbeddingProvider` is not authorized for evaluation** merely because it exists for retrieval | RO-18 | **Approved** (Repository Owner) |
| **RO-18** D-8 — metric semantics | Does RO-18 fix the algorithms? | **No — the semantics are derived by M2-08 from the authoritative material** (`docs/AI_Quality_Metrics_Reference.md`; `docs/altm.md` §4, §5, §6; `docs/GENERATION_CONTRACT.md`) and **the exact operational propositions recorded in implementation evidence before implementation.** **The reconnaissance report is NOT authoritative for metric definitions** — it is evidence about framework capability, dependencies, architecture, provider requirements, telemetry and compatibility, and nothing else; **generic framework terminology MUST NOT be adopted as a definition.** **Four boundaries MUST survive:** **Faithfulness** — claims supported/entailed by the available retrieved context; **Groundedness** — substantive claims traceable to the appropriate evidence passages, **preserving the whole-context vs per-passage distinction**; **Hallucination Rate** — **derived from the repository's claim-level grounding/faithfulness semantics**, no unrelated formula invented; **Answer Relevancy** — whether the answer addresses the question, **not collapsed into correctness or faithfulness.** **M2-08 MUST preserve enough evidence to explain a result**; **no evidence schema, report format or trace shape is prescribed**, and **no threshold, target or acceptance score is invented.** **No quality claim is created** — `docs/GENERATION_CONTRACT.md` §24.3's *"neither established nor claimed here"* stands | RO-18 | **Approved** (Repository Owner) |
| **RO-18** D-9 — layer progression | Does M2-08 supersede the earlier metrics? | **No. The layers are not collapsed and answer different questions.** Milestone 1's deterministic retrieval metrics (Hit Rate, Chunk Precision@K, Chunk Recall@K, coverage classification) ask *"did retrieval find the expected evidence?"*; **M2-07** asks whether the retrieved context is relevant and contains the independently-authored evidence; **M2-08** asks whether generation was grounded, faithful and relevant; **M2-18** supplies execution evidence that explains why. **The newer metrics MUST NOT redefine the older ones**, **M2-08 does not supersede the Milestone 1 metrics**, **M2-07 does not supersede Chunk Precision@K / Chunk Recall@K** — `docs/P3.3.3_…` §3's non-proxy reservation runs in both directions — and **the repository retains both deterministic and LLM-judged metrics.** **No Milestone 1 metric is renamed or reinterpreted** | RO-18 | **Approved** (Repository Owner) |
| **RO-18** D-10 — **M2-10** | Does the native M2-08 path discharge **M2-10**? | **No. M2-10 remains separate and OPEN** — not discharged, row not edited, status unchanged. **Producing M2-08 metric results later does not automatically discharge it**; **RO-16 D-8** and **RO-17 D-7** both recorded that a sprint which measures is not thereby a sprint which discharges the measurement capability, and that reasoning applies unchanged. **No existing precedent requires a strictly necessary M2-10 row synchronization here**, and none is performed | RO-18 | **Approved** (Repository Owner) |
| **RO-18** D-11 — **M2-07** | Is M2-07 affected? | **No. M2-07 remains exactly as implemented and governed, and is not reopened.** Its **Context Precision / Context Recall semantics are unchanged**; `evaluation/context_metrics.py`, `tests/test_context_metrics.py`, `scripts/evaluate_context.py` and `docs/M2.07_Native_Context_Metrics_Report.md` (commit `6543bae`) are **untouched**; **RO-17 is unamended in every part**; and **M2-07 is not redesigned or rerun.** **No retrieval optimization is authorized on the strength of any M2-07 score**, and **RO-18 asserts no retrieval-quality or context-quality claim**. ▶ **The decision above is retained as issued and is accurate as the account of what RO-18 ruled at commit `6543bae`.** `scripts/evaluate_context.py` was **subsequently** modified at commit `42bcadb` (CLI selection guard only); `evaluation/context_metrics.py`, `tests/test_context_metrics.py` and `docs/M2.07_Native_Context_Metrics_Report.md` remain **byte-identical** and the Context Precision / Context Recall semantics are **unchanged**. Recorded at §5; adjacent note at `docs/DEFERRED_ITEMS_REGISTER.md` §4.10 Decision 11 | RO-18 | **Approved** (Repository Owner) |
| **RO-18** D-12 — **M2-18** | Is M2-18 affected? | **No. M2-18 remains ✅ DISCHARGED and RO-15 is unamended and not reinterpreted.** `scripts/execution_trace.py`, `scripts/run_generation.py`, `tests/test_execution_trace.py` and `docs/M2.18_Execution_Evidence_Report.md` are **not modified**, **no trace field may be added merely to make M2-08 easier**, no `VectorStore` widened and no semantic similarity score exposed. **M2-08 MAY consume trace evidence diagnostically** — RO-15 D-2's explainability — but **M2-18 is not a prerequisite, may not be redesigned, and RO-18 creates no new trace capability** | RO-18 | **Approved** (Repository Owner) |
| **RO-18** D-13 — implied authority | Does RO-18 authorize anything beyond the native M2-08 path and the narrow judge seam? | **No.** Not retrieval optimization, chunking, corpus deduplication, embedding, vector-store, RRF or top-k changes; not prompt optimization, generation-model changes, provider-architecture changes, model routing or agent frameworks; not **LangChain**, **DeepEval**, **Promptfoo** or **Ragas**; not production observability, **OpenTelemetry** or dashboards; and not **M2-10**, **M3-01** or **M3-06** discharge. Each remains governed by its own existing authority or a future ruling | RO-18 | **Approved** (Repository Owner) |
| **RO-18** — implementation | Is M2-08 implemented or discharged by this ruling? | **No. RO-18 authorizes the path; M2-08 determines the implementation inside it.** **Nothing was installed, imported, executed or measured** — `deepeval`, `promptfoo`, `ragas`, `langchain` and `openai` are all absent, `requirements.txt` is byte-identical, and `sample_rag/`, `scripts/`, `tests/`, `evaluation/`, `datasets/` and `.gitignore` are untouched. **No judge call, no credential read, no network access.** **It discharges nothing** — **M2-07**, **M2-08**, **M2-10**, **M3-01** and **M3-06** remain **OPEN**, **M2-18** stays ✅ DISCHARGED — **allocates nothing** (no register row added **and none edited**, per **RO-10 D-2**; the owed M2-08 row synchronization is carried as a §4.10 Finding with proposed language for Repository Owner review), and **creates no capability, milestone or identifier** | RO-18 | **Authorized, unimplemented** (Repository Owner) |

**M2-07 itself is deliberately absent from the table above as a work item**, for the reason the M2-18 note below gives: what is recorded here are **dispositions already made** — the Repository Owner decisions RO-16 took — while the capability **M2-07**, being authorized-but-unbuilt, is **scheduled work** and belongs in `docs/DEFERRED_ITEMS_REGISTER.md` §4, where its row lives. §1.2's governing test bars it from this register. **No implementation evidence is recorded here, and none exists.**

**M2-18 itself is deliberately absent from the table above as a work item.** What is recorded here are **dispositions already made** — the Repository Owner decisions RO-15 took — which is what §1.1 responsibility 4 and §1.2's governing test admit. **The capability M2-18, being allocated-but-unbuilt, is scheduled work and belongs in `docs/DEFERRED_ITEMS_REGISTER.md` §4**, which is where its row lives; §1.2 bars it from this register, exactly as it bars U-3 below. **No implementation evidence is recorded here, and none exists.**

**U-3 is deliberately absent from the table above.** It remains **open** — `REACHABLE_STAGES` is not widened, no orchestration layer is authorized, and `scripts/cli.py` is unchanged. RO-13 defines what the Generator contract *is*; **M2-06 determines how the runtime reaches it**, and evidences it there. Recording an unresolved question here would misstate it as a disposition, and **U-3 is not converted into a capability** — §1.2's governing test bars scheduled work from this register. The related structural observation is already carried as **M2.12-F-1** (§3.5), which RO-13 does **not** resolve.

**Consequence recorded, not actioned.** §22 assigns the corresponding `docs/architecture.md` §5 `Generator` row amendment to the Repository Owner, in the manner `docs/MILESTONE_1A.md` build item 4 amended the `Retriever` row. It is unperformed at Sprint P3.7.2 and is carried in `docs/P3.7.2_Repository_Governance_Synchronization_Report.md` §5 as a deferred item, not as a finding.

**Standing governance rule**, established at Sprint P3.1.7.1 and carried forward: architectural disposition is not the implementing agent's decision. An implementing agent presents options and a recommendation; a recommendation is not a decision.

---

## 3. Findings register

### 3.1 F-1 — Duplicate manifest identifiers accepted silently

| | |
|---|---|
| **Raised** | Sprint P3.1.5 (Construction Validation) |
| **Verified** | Sprint P3.1.7.1 — **CONFIRMED**, independently reproduced: a manifest with two entries sharing `id: "dup"` returns two `Document`s sharing that id, no exception |
| **Evidence** | `sample_rag/knowledge_source.py` `load()` — no uniqueness check across entries |
| **Disposition** | **DEFERRED to Sprint P3.1.8 (Data Quality Validation)** — upheld at P3.1.7.1 and unchanged at P3.1.7.2 |
| **Why deferred** | Uniqueness is a collection-level, cross-artifact property. `docs/DOCUMENT_CONTRACT.md` §8.5 routes such checks to Data Quality Validation; it is not among §8.7's three invariants |
| **Coupled to** | **D-2** (§3.6) — **resolved** at Sprint P3.1.8.0B by Contract Erratum E-1 (`docs/DOCUMENT_CONTRACT.md` §8.9). The invariant F-1's check must enforce is now stated, so the check no longer risks inventing one |
| **Implementation** | Contract Erratum E-1 stated the invariant; Sprint P3.1.8.1B shipped the DQ-2 uniqueness specifications at commit `e9405ad`. `sample_rag/knowledge_source.py` is unchanged — a duplicate identifier is **detected** by Data Quality Validation, not **prevented** by `load()` (§8.9 item 5) |
| **Specifications** | `tests/test_data_quality.py` — 4 DQ-2 specifications: W2 predicates A and B, each with a synthetic duplicate-id case reproducing this finding's recorded shape |
| **Mutation evidence** | §6 — mutants **M20**, **M21**, **M22** killed by the DQ-2 specifications. **M21** is killed by `test_dq2_loaded_document_ids_are_pairwise_distinct`, the predicate the specification records as vacuous on the one-document corpus: it is vacuous against a duplicate *Manifest entry*, not against a `load()` that duplicates a `Document` |
| **Status** | **Closed** at Sprint P3.1.8.4, under the event-driven closure policy (§8). The trigger this entry documented — *"Sprint P3.1.8.1 ships the DQ-2 uniqueness specification"* — occurred at `e9405ad`. Sprint P3.1.8.1's own completion remains governed independently by `docs/DATA_QUALITY_VALIDATION_PLAN.md` §13 |

### 3.2 F-2 — Corpus-root containment not enforced

| | |
|---|---|
| **Raised** | Sprint P3.1.5 (Construction Validation) |
| **Verified** | Sprint P3.1.7.1 (EV-3) — **CONFIRMED**, both vectors reproduced: an absolute `documents[].source` and a `..` relative escape each loaded a file from outside `sample_rag/` and returned its contents as `Document.text` |
| **Disputed** | Codex review → defer to P3.1.8. Claude Code review → Construction, remediate first. P3.1.7.1 declined to resolve and escalated it |
| **Disposition** | **RESOLVED at Sprint P3.1.7.2** by [`ADR-P3.1.7.2-F2`](adr/ADR-P3.1.7.2-F2-corpus-root-containment.md), Option A — Construction |
| **Implementation** | `resolve_source_path` rejects an escaping source as an Input failure (§10.1) |
| **Specifications** | `tests/test_knowledge_source_failures.py` — 6 AH-7 specifications |
| **Status** | **Closed** |

### 3.3 Findings dispositioned at Sprint P3.1.7.2

Every finding the Sprint P3.1.7.1 Decision Gate classified as CONFIRMED, with the repository change that resolved it.

| Finding | Decision Gate status | Change | Evidence of closure |
|---|---|---|---|
| **EV-2 / I-1** — `Document.text` content unspecified; a mutation emptying it left 77/77 passing | CONFIRMED | AH-1: 4 content specifications (`.docx`, `.md`, `.txt` round-trips; real-corpus substantive-text) | Mutation `w:t findall` (empties text) now **KILLED**, 5 failures |
| **EV-1 / I-2** — rule N1 unprotected; 86 blocks → 1, Chunker driven to fallback | CONFIRMED | AH-2: 3 specifications including the full `.docx` → N1 → `Document.text` → Chunk-boundary chain | Mutation `PARAGRAPH_SEPARATOR="\n"` now **KILLED**, 4 failures |
| **EV-3 / A-2** — corpus-root containment | CONFIRMED | AH-7 (see §3.2) | Removing the check → **KILLED**; weakening it to absolute-only → **KILLED** |
| **EV-4 / D-1** — P3.1.5 evidence report not valid UTF-8 | CONFIRMED (escalated) | AH-5: structural restoration + Restoration Record | File is valid UTF-8; 6 structural hunks, **zero prose changes** |
| **I-3** — unreachable defensive guard, module's only uncovered line | CONFIRMED | AH-8: guard removed | `sample_rag/knowledge_source.py` coverage **100%** |
| **I-4** — no synthetic `.docx` success spec; `.md` never exercised | CONFIRMED | AH-1 (covers both) | `SyntheticCorpus.docx()` now invoked; `.md` fixture added |
| **I-5** — empty-text legality unspecified through `load()` | CONFIRMED | AH-3: 4 specifications | Mutation rejecting empty text now **KILLED** |
| **A-1** — `SUPPORTED_EXTENSIONS` duplicated, no spec asserting agreement | CONFIRMED | AH-9: cross-boundary equality specification | Widening one definition now **KILLED** |
| **D-3** — normalization rules existed only in source | CONFIRMED | AH-4: `DOCUMENT_CONSTRUCTION_PLAN.md` §20.1 | N1–N5 documented with rationale |
| **D-5** — "full plain-text content" vs. a normalizing extractor | CONFIRMED | AH-4: §20.2 clarification | Recorded as clarification, contract unchanged |
| **D-4** — plan status and §14.2 sync list stale | CONFIRMED | AH-6: status header + §14.2 corrected | Sync items marked complete at `8839802` |
| **G-1** — Decision Register never closed | CONFIRMED | §20.3 closure table | All Sprint-P3.1 decisions closed |
| **G-2** — P3.1.5 evidence committed under the P3.1.6 commit | CONFIRMED | Recorded in the Restoration Record and §5 below | Provenance documented |
| **G-4** — no mutation or coverage evidence in the repository | CONFIRMED | §6 below | Baseline recorded |

### 3.4 Findings closed without repository change

| Finding | Disposition | Rationale |
|---|---|---|
| **NEW-2** — "6 of 18 mutants survive" overstated the specification gap | **PARTIALLY CONFIRMED**, no change | Sprint P3.1.7.1 established M10 and M14 as **equivalent mutants** (see §6). The correction is the action |
| **NEW-1** — Codex's claim that the specifications "protect the approved behavior" | **REJECTED**, no change | Not supported: that review performed no mutation testing, and EV-1/EV-2 disprove the claim as unqualified. Its other findings each verified independently. A review report is an input, not a repository artifact |
| **D-6 / P3.1.7-DOC-01** — contract retains "proposed" wording | Confirmed, **no action** | Cosmetic; already self-disclosed in the contract's own Correction Record. §8.2–§8.7 are deliberately byte-for-byte frozen |
| **G-5** — dependency governance gate | Confirmed **closed correctly** | Construction is stdlib-only; `requirements.txt` unchanged, so §12.3 step 2 was never owed |
| **A-4** — no injection seam for the corpus root | **REJECTED as actionable** | Design observation; no defect observed at current scope |
| **M2.12-F-4** — the deferred register's **M2-12** rationale reads as if M2-12 sequenced *after* M2-06, while `docs/roadmap.md` §1.1 sequences it *before* | **NOT a discrepancy**, no change | The two documents answer different questions, and the split is the repository's existing authority model rather than a conflict. `docs/DEFERRED_ITEMS_REGISTER.md` §4.2 **R-M2-12/13**'s *"neither has a referent until M2-06"* answers *which milestone*, its stated question; `docs/roadmap.md` §1.1, which owns *stage within Milestone 2* under ruling **RO-07**, answers *sequence within it*. Register §1.2 bars that register from carrying sequence, so the passages cannot conflict. **The existing authority model is preserved and no document was edited** — manufacturing a governance discrepancy here would be the error. The substantive half of the observation, that M2-12's artifact has no consumer until M2-06, is recorded structurally as **M2.12-F-1** in §3.5. Surfaced as finding **F-4** of `docs/M2.12_Context_Builder_Report.md` §9; recorded here at the M2-12 discharge |

### 3.5 Open findings and observations

Recorded because they have a disposition, not because they are scheduled.

**On the `M2.01B-F-1` identifier.** Sprint-report findings are numbered per report, so M2.01B's **F-1** is not this register's **F-1** (§3.1, duplicate manifest identifiers) and its **F-2** is not §3.2 (corpus-root containment). The row below is therefore qualified by its originating sprint, following the qualification this table already uses for `F-2-sym` and `P3.1.7-ARCH-01`. **The four §3.1 / §3.2 records keep their identifiers unchanged** — CP-3 governs, and nothing historical is renumbered.

| ID | Observation | Disposition |
|---|---|---|
| **F-2-sym** | Containment reads the manifest value, so a corpus file that is a **symlink** pointing outside the root is not detected | Deliberate boundary of `ADR-P3.1.7.2-F2`. Candidate for Data Quality Validation if evidence ever emerges; none exists today |
| **I-6** | `test_b6` hardcodes the corpus filename `Karthik_SR_Resume_v2_2.docx` | Re-verify at corpus expansion |
| **I-7** | `test_a15`'s allowlist tracks CPython-synthesized dataclass members (`__firstlineno__`, `__static_attributes__` are 3.13+; suite runs on 3.12) | Re-verify at the next CPython upgrade |
| **A-3** | `discover_manifest_entries` performs admissibility checks bounded only by a docstring against growing into a second `validate_manifest` | Re-inspect if that function grows |
| **P3.1.7-ARCH-01** | JobOps-as-`Document` classification unresolved (Contract Outstanding Question 3) | Intentionally deferred; structurally excluded today by the manifest discovery gate |
| **M2.01B-F-1** | **Index embedding-provider provenance.** `Index` (`sample_rag/indexer.py`) does not intrinsically carry embedding-model/provider identity — it holds `vectors`, `dimension` and `stub`, and nothing recording what embedded them. A caller can therefore theoretically pair an Index built by one provider with a different provider at persistence time, and record a false model identity in the vector-index metadata | **Future hardening — not a current defect.** No repository path exhibits it: the one production caller supplies the same provider it indexed with, and `docs/architecture.md` §7 freezes `EmbeddingProvider` at a single `embed` method, so M2.01B read identity defensively via `getattr` rather than growing the seam. Revisit whether provider/model identity should become part of the Index contract, so a mismatched provider/index pairing is detectable rather than merely improbable. **No implementation is owed by this entry**, and it allocates no capability and changes no milestone. Surfaced as finding **F-1** of `docs/M2.01B_FAISS_VectorStore_Foundation_Report.md` §15; recorded here at Sprint RO-09 |
| **M2.03-F-1** | **BM25 length normalization interacts badly with header-sized chunks.** The structure-aware chunker emits section headers as their own chunks — 48 of 259 chunks are ≤ 3 tokens, against a corpus mean of 17.2 — while the Evidence Trace Dataset's expected-evidence chunks average 37.6 tokens. With `b = 0.75`, BM25's length normalization can therefore rank a short header carrying one query term above a substantially longer body chunk carrying more query terms, and retrieval quality against the 22 committed expectations fell (hit rate 0.7273 → 0.4091; Chunk Precision@K micro 0.1481 → 0.0833) even though the ranking function is correct | **Corpus/tuning finding, not a BM25 defect** — every reported score is independently verified against the published formula (`tests/test_lexical_bm25.py`). **M2-04 is the next architectural checkpoint for this finding**, because it combines the independently validated semantic and lexical routes through RRF; the M2.03 evidence demonstrates that the routes have complementary retrieval characteristics, including cases where BM25 cannot recover paraphrased evidence that the semantic route can. **However, M2-04 is NOT designated by this entry as a guaranteed remedy.** Whether RRF actually improves, partially recovers, or fails to recover the observed regression must be established by M2-04's own implementation and retrieval-quality evidence. If the finding remains materially unresolved after M2-04, the appropriate subsequent candidates remain **M2-15** — retrieval-quality optimization, including `k1`/`b` tuning against an appropriate quality signal rather than tuning directly against the current 22-question Evidence Trace benchmark — and **M2-17** — chunk-size/granularity revision addressing the underlying corpus structure. **Revisit M2.03-F-1 after Sprint M2-04 completes and its own retrieval-quality measurements are available.** **No capability is allocated by this finding**, and **no implementation is owed by this entry**: **M2-15 and M2-17 remain independently scoped** in `docs/DEFERRED_ITEMS_REGISTER.md` §4, and neither is triggered automatically by this entry. Surfaced as finding **F-1** of `docs/M2.03_Real_BM25_Lexical_Retrieval_Report.md` §15; recorded here at Sprint RO-11. **▶ REVISITED at Sprint RO-12, as RO-11 required — outcome CASE B, improved but insufficient; the finding remains PARTIALLY UNRESOLVED. See *M2.03-F-1 revisited* below.** |
| **M2.04-F-1** | **A specification equated filesystem absence with Git non-tracking.** `tests/test_vector_query.py::test_m201c_the_runtime_artifact_is_not_a_repository_source_artifact` asserted that the FAISS index and its metadata were **absent from disk** at `sample_rag/vector_index/`, while its own docstring stated the intent as RO-09 items 3–5 state it — generated FAISS artifacts are **not committed**. Those are different properties. The artifact is derived runtime state, it is `.gitignore`d, and RO-09 items 6 and 9 positively expect it to exist at that runtime location, so filesystem presence was never the policy being enforced. The specification therefore passed only while no repository path had executed `VectorIndexRuntime` at its default location — every M2.01C specification uses `tmp_path` — and Sprint **M2.04** was the first path that did, via `scripts/run_hybrid_retrieval.py` | **RESOLVED by Repository Owner ruling RO-12** (`docs/DEFERRED_ITEMS_REGISTER.md` §4.4, Decision 2), **through correction of the specification assertion**. The corrected specification verifies that **nothing under `sample_rag/vector_index/` is tracked by Git**, and keeps the `.gitignore` half unchanged; the artifact is now permitted to exist at the runtime location, as the policy always intended. **The defect was in the M2.01C specification assertion, NOT in the M2-04 fusion implementation** — no file authored by Sprint M2.04 is involved, and the failure was caused solely by the presence of the derived artifact. **Not an M2-04 implementation defect; not an architecture issue.** `VectorIndexRuntime`, the FAISS lifecycle, `.gitignore`, **RO-09** and `docs/M2.01C_Semantic_Query_Foundation_Report.md` are all unchanged, and the corrected assertion is a **present correction** — it is not represented as having existed during Sprint M2.01C. Surfaced as finding **F-1** of `docs/M2.04_RRF_Fusion_Report.md` §12; recorded here at Sprint RO-12 |
| **M2.12-F-1** | **The Context Builder has no runtime consumer.** Sprint M2.12 discharged **M2-12** at an independently testable seam and wired nothing to reach it: the repository's only retrieval→generation caller is `scripts/cli.py`, whose chain is `parse -> load_corpus -> Retriever.retrieve -> Generator.generate -> serialize`. That path **does not use fusion at all** — it runs the lexical route directly — and its `Generator` consumes a `RetrievalResult`. Wiring `RRF → ContextBuilder → Generator` into it would have changed that path's retrieval route **and** its generator input, the second of which `docs/GENERATION_CONTRACT.md` §17/§22 **G-2** freezes. `test_m212_no_pipeline_module_imports_the_context_builder` records the absence structurally rather than by prose | **Deliberate, and not a defect** — it is the same discipline `docs/GENERATION_CONTRACT.md` §6.2 applied when it rejected *"Generator constructs its own prompt internally"*. **No orchestration layer was created, and none is owed by this entry.** The question of how a `Prompt` reaches a `Generator` is the Generator input transition, which `docs/GENERATION_CONTRACT.md` §6.2 records as *"the Milestone 2 target, reached when a Context Builder exists"* and which **M2-06** owns (with the `docs/architecture.md` §5 `Generator` row owned by **M2-14**). **This entry allocates no capability, owes no implementation, changes no milestone, and triggers neither M2-06 nor M2-14** — M2-06 is independently scoped in `docs/DEFERRED_ITEMS_REGISTER.md` §4. Surfaced as finding **F-1** of `docs/M2.12_Context_Builder_Report.md` §9; recorded here at the M2-12 discharge |
| **M2.12-F-3** | **`docs/architecture.md` §8's sequence diagram is not synchronized with the amended §5 `Generator` row.** §8 shows `CB->>G: Assembled prompt`, while the §5 row — amended at Sprint P3.7.4 under authorization **A5**, per `docs/GENERATION_CONTRACT.md` §22 **G-2** — now reads `generate(query, retrieval: RetrievalResult)` with dependency *Retriever*. §5's own synchronization note already records the §7 Protocol sketch as a deliberate known divergence for the same reason; **the §8 divergence is recorded in no committed authority**, which is why it is recorded here | **Reported, not resolved — no document was edited.** Amending `docs/architecture.md` is a Repository Owner action: the precedent is `docs/GENERATION_CONTRACT.md` §22, discharged at Sprint P3.7.4 under authorization **A5**, and P3.7.4 itself deferred the neighbouring `Context Builder` row question as **R-3** (*"No P3.7.3 authorization covers this row … Requires Repository Owner judgement"*) rather than inferring authority for it. **Discharging M2-12 does not resolve it and did not touch §8**, and rewriting a historical architecture diagram to make a later discharge read cleanly is precisely what **CP-3** forbids. **This entry allocates no capability and owes no implementation**; it records a documentation divergence awaiting Repository Owner judgement, alongside the already-recorded **R-3** and the §5 `Context Builder` row's own residual-divergence note. Surfaced as finding **F-3** of `docs/M2.12_Context_Builder_Report.md` §9; recorded here at the M2-12 discharge |
| **M2.06-F-4** | **Two generator components coexist, and RO-13 did not dispose of the first one.** Sprint M2.06 implemented v2.0.0's `generate(prompt: Prompt) -> GenerationResult` in a **new** module, `sample_rag/model_generator.py`, leaving `sample_rag/generator.py` byte-identical: replacing in place would have put a live provider call inside the deterministic pytest suite and retired the byte-identical acceptance evidence `docs/P3.7.6_…` §4 froze. The class was named `ModelGenerator` rather than `Generator` because *"two classes named `Generator` both claiming the `docs/architecture.md` §5 row would be a worse outcome than a name that does not claim it."* Which component **is** that row was left undecided — RO-13 §24.2 authorized the row synchronization and explicitly did not discharge it | **RESOLVED by Repository Owner ruling RO-14** (`docs/DEFERRED_ITEMS_REGISTER.md` §4.6, Decision 1), **in favour of the dual-path architecture**: `Generator` is the frozen Milestone 1A deterministic / reference component and `ModelGenerator` is the Milestone 2 model-backed component, and **both are authorized architectural components**. The intervening **Sprint M2-14 STOPped and modified nothing**, correctly declining to select between the alternatives without Repository Owner authority — the standing rule of §2, *"architectural disposition is not the implementing agent's decision."* **RO-14 supersedes RO-13 Decision 5 only as to its singular-row synchronization target** and **widens M2-14** to own the resulting `docs/architecture.md` synchronization. **This entry allocates no capability and owes no implementation** — M2-14 is independently scoped in `docs/DEFERRED_ITEMS_REGISTER.md` §4, remains **OPEN**, and **M2-06 remains OPEN and is not discharged by RO-14.** No runtime file, test or architecture document was changed by the resolution. Surfaced as finding **F-4** of `docs/M2.06_Generation_Report.md` §9; recorded here at Sprint RO-14 |
| **M2.06-F-2** | **An interpretive tension inside §24.** §24.3 states that *"§8's field definitions"* are unchanged, while §8.1 defines `answer_text` on the Answer path as *"assembled from the `statements` below and contains no content not present in them"* — and the same §24.3 states by name that under v2.0.0 `answer_text` *"MAY be synthesized by the model"* and that *"the quotation-only derivation requirement no longer applies to it."* Sprint M2.06 applied the more specific clause and reported the tension *"for confirmation"* rather than resolving it as governance | **RESOLVED by Repository Owner ruling RO-14** (`docs/DEFERRED_ITEMS_REGISTER.md` §4.6, Decision 5): **the specific G-7 transition governs.** `answer_text` may be model-synthesized under v2.0.0; the structural field is unchanged; §8.1 is **not edited** and remains accurate for v1.0.0; `GeneratedStatement.text` remains a verbatim quotation of its own span; `SupportingEvidence` remains grounded in the assembled `Prompt` context and provenance. **No faithfulness, groundedness, hallucination-absence or answer-relevancy guarantee is created**, and §21's exclusion of the Layer 3/4 metric set stands — empirical evaluation remains **M2-07** / **M2-08** work. Recorded in the contract at `docs/GENERATION_CONTRACT.md` **§25.1** through the adjacent-erratum mechanism `docs/DOCUMENT_CONTRACT.md` §8.9 established, with **§1–§24 byte-for-byte unchanged and no new contract version**. **The implementation was not changed** — RO-14 confirms the reading M2.06 applied. **This entry allocates no capability and owes no implementation.** Surfaced as finding **F-2** of `docs/M2.06_Generation_Report.md` §6.2; recorded here at Sprint RO-14 |
| **M2.06-F-1** | **The provider offers no evidence-attribution mechanism, and none was invented.** DeepSeek's chat-completion contract carries **no citation or evidence-attribution field**: no part of the response identifies which supplied span a sentence came from. Splitting the model's prose into claims and attributing each to a chunk would therefore have been Sprint M2.06 *inventing* an attribution the provider never made — *"an invented citation is worse than none, because it is checkable-looking."* What was implemented instead is the smallest mapping Generation Contract **v2.0.0** admits: one `GeneratedStatement` per assembled chunk, each carrying exactly the one `SupportingEvidence` that chunk contributed, built from `Prompt.context` and `Prompt.provenance` **before** the provider is called | **Deliberate, and not a defect — it is the surviving half of G-7, satisfied exactly as §24.3 states it.** §24.3 narrows G-7 for `answer_text` alone and requires `SupportingEvidence` to *"remain grounded in the assembled `Prompt` context and provenance"* and in nothing else, which is what the implementation does; **no provider citation capability is fabricated, and none is owed by this entry.** What the artifact asserts is exactly *"these spans were assembled, and this answer was produced with them present"* — **not** that the answer uses the evidence, is entailed by it, or contains nothing beyond it. Those are **Faithfulness** and **Groundedness**, are Layer 3/4 metrics, and are §21-excluded and RO-13-disclaimed; establishing them empirically is **M2-07** / **M2-08** work, which this entry does not trigger. **RO-14 expressly did not dispose of this finding** (`docs/DEFERRED_ITEMS_REGISTER.md` §4.6), and **the M2-06 discharge does NOT resolve it** — M2-06 is discharged at its implementation boundary with this finding **open and non-blocking**. **This entry allocates no capability, owes no implementation and changes no milestone.** Surfaced as finding **F-1** of `docs/M2.06_Generation_Report.md` §6.2; recorded here at the M2-06 discharge |
| **M2.06-F-3** | **The Abstain path is unreachable through the hybrid runtime route, for a retrieval-side reason.** Sprint M2.06's second real-provider acceptance run used a question sharing no term with the corpus (*"zzzz qqqq wwww"*) and still assembled five chunks: the **semantic route returns `top_k` nearest neighbours regardless of distance**, so the fused route is never empty and the Generator's abstention predicate never fires on this path. The model itself replied that the context did not contain an answer, and the outcome was correctly still `Answer` — the corpus *did* supply evidence, and `docs/GENERATION_CONTRACT.md` §9.3 decides outcome from the `Prompt`, never off model prose | **A property of dense retrieval without a relevance threshold, not a generation defect — and it was NOT fixed.** The abstention path itself is implemented and correct, and is specified against an empty `Prompt` by `test_m206_an_empty_prompt_abstains_without_calling_the_provider`, including that it performs **no** provider call. Adding a similarity floor would be **retrieval tuning**, which M2-06 was barred from; the candidates are **M2-05** (reranking) and **M2-15** (retrieval-quality optimization), both independently scoped in `docs/DEFERRED_ITEMS_REGISTER.md` §4 and **neither triggered by this entry**. **RO-14 expressly did not dispose of this finding** (§4.6), and **the M2-06 discharge does NOT resolve it, does not mark it fixed, and creates no retrieval capability for it** — it belongs to the retrieval milestone that owns route tuning and remains **open and non-blocking** to M2-06. **This entry allocates no capability, owes no implementation and changes no milestone.** Surfaced as finding **F-3** of `docs/M2.06_Generation_Report.md` §7; recorded here at the M2-06 discharge |
| **M2.18-F-1** | **No temporal or execution-identity evidence is recorded, because the repository has no convention for either.** A search of `sample_rag/` and `scripts/` returns **no** `datetime`, `utcnow`, `isoformat` or `timezone.utc` reference: the repository holds no wall-clock convention, and no execution-identity convention exists to follow either. Sprint M2.18 therefore recorded **neither** a timestamp nor a synthetic execution id, and specified both as absent so the omission cannot be undone silently. **Consequence:** trace records are distinguished by append order and by `query`, and two runs of the same question are not distinguishable from a record alone | **Compliance with RO-15, not a shortfall against it.** RO-15 Decision 5 authorizes *"execution identity; timestamp"* expressly **"where available at an existing boundary"**, and requires that an item which *"proves unavailable at an existing boundary is **omitted and reported**, not engineered into existence."* Neither is available, both were omitted, and this entry is the report. Manufacturing a clock contract or a generated label to populate the field is precisely what that Decision bars, and **a generated identifier is not evidence about the execution** in any case. **Adding either is a Repository Owner decision about repository convention**, not an implementation gap M2.18 was permitted to fill. **This entry allocates no capability, owes no implementation and changes no milestone**; it does **not** trigger a timestamp, clock, identity or serialization capability. **Open and non-blocking to the M2-18 discharge.** Surfaced as finding **F-1** of `docs/M2.18_Execution_Evidence_Report.md` §22; recorded here at the M2-18 discharge |
| **M2.18-F-2** | **No concurrency, locking, rotation or retention behaviour exists.** The implementation appends one line per completed execution in text-append mode, with no lock file, no rotation, no retention policy and no cross-process coordination. `scripts/run_generation.py` performs one execution per invocation, so the repository does not currently produce a concurrent writer | **An exercise of a delegation RO-15 granted, not an omission against it.** Decision 4 lists *"concurrency and locking semantics, file rotation, retention policy, filename conventions, directory path, exact field ordering, key naming, and nesting shape"* as **"deliberately NOT prescribed, and belonging to the M2-18 implementation sprint"** — *"RO-15 fixes the representation family; M2-18 fixes the representation."* The simplest repository-native choice was taken and recorded rather than solved speculatively against a writer the repository does not produce. **Should a concurrent or long-running writer ever exist, this entry is where that changed premise is already recorded** — it does **not** owe an implementation now, allocates no capability, and creates no retention, rotation or telemetry capability. **Open and non-blocking to the M2-18 discharge.** Surfaced as finding **F-2** of `docs/M2.18_Execution_Evidence_Report.md` §22; recorded here at the M2-18 discharge |
| **M2.18-F-3** | **The frozen Milestone 1A path is not traced.** `scripts/cli.py` (`Generator`, contract **v1.0.0**) writes no trace, and `scripts/execution_trace.py` `CONTRACT_VERSIONS` deliberately carries **no** v1.0.0 entry — an unrecognized component raises rather than being given a guessed contract era. Only the Milestone 2 model-backed path (`ModelGenerator`, **v2.0.0**, `scripts/run_generation.py`) is traced | **Required by RO-15, not merely permitted — tracing the M1A path would have violated the ruling.** Decision 3 states that the envelope *"does not touch the frozen Milestone 1A path"* preserved by **RO-14 Decision 2**: *"`scripts/cli.py` stays on `Generator`, the frozen specification counts stand, and no provider call may enter the deterministic pytest suite."* The untraced M1A path is therefore a **satisfied constraint**, not an incomplete one, and `tests/test_generator.py` (**48**) and `tests/test_cli.py` (**27**) are unchanged, as is `scripts/cli.py`. **Recording a v1.0.0 era would describe an execution the repository does not produce.** **This entry allocates no capability and owes no implementation**, and extending tracing to the frozen path would require a Repository Owner decision that RO-15 expressly withheld. **Open and non-blocking to the M2-18 discharge.** Surfaced as finding **F-3** of `docs/M2.18_Execution_Evidence_Report.md` §22; recorded here at the M2-18 discharge |
| **M2.18-F-4** | **Statement-to-evidence attribution is currently 1:1, because the generator's attribution is.** `ModelGenerator._statements` emits one `GeneratedStatement` per assembled chunk, each carrying exactly the one span that chunk contributed, so the recorded `statement_evidence_chunk_ids` currently mirrors the assembled provenance order. This is the generation component's behaviour, not a choice the trace layer made | **A downstream consequence of `M2.06-F-1`, which this discharge does NOT resolve.** DeepSeek's chat-completion contract carries no citation or evidence-attribution field, so M2-06 declined to invent one — *"an invented citation is worse than none, because it is checkable-looking."* The trace **records the linkage structurally rather than assuming it**: `statement_evidence_chunk_ids` is a per-statement list of cited chunk ids, so a future generator that attributed two chunks to one statement, or none, would show that difference with **no change to the trace layer**. **M2-18 makes M2.06-F-1's consequence observable, and RO-15 Decision 11 fixes what that does and does not mean**: *"it may make their behaviour more observable, and observability is not repair."* **M2.06-F-1 remains open and unresolved**, is **not** marked fixed by the M2-18 discharge, and **this entry allocates no capability, owes no implementation and triggers no provider, citation or attribution capability.** **Open and non-blocking to the M2-18 discharge.** Surfaced as finding **F-4** of `docs/M2.18_Execution_Evidence_Report.md` §22; recorded here at the M2-18 discharge |

**On M2.03-F-1's four levels of claim**, kept deliberately distinct because collapsing them would convert a measurement into a plan:

| Level | Statement |
|---|---|
| **Fact** | BM25 currently performs worse on the committed 22-question benchmark than the distinct-term overlap scorer it replaced |
| **Fact** | The degradation has a demonstrated relationship to the corpus chunk-length distribution and BM25's length normalization — a sweep over `b` alone, holding the implementation fixed, is monotone (`docs/M2.03_…` §15 F-1) |
| **Fact** | The BM25 scores are mathematically correct for the selected formula, verified by independent recomputation rather than by asserting the implementation against itself |
| **Architectural hypothesis — not yet proven** | That fusing the semantic and lexical routes at **M2-04** recovers some of the regression. M2-04 is where this is **tested**, not where it is assumed; **M2-15** and **M2-17** are future options if the finding survives that test |

**The fourth level was tested at Sprint M2.04.** The table above is RO-11's record and stands as written; the outcome of the test it names is recorded immediately below rather than substituted into it.

#### M2.03-F-1 revisited at Sprint RO-12 — **CASE B: improved but insufficient / partially unresolved**

**The RO-11 revisit condition is satisfied.** RO-11 required this finding to be revisited *"after Sprint M2-04 completes and its own retrieval-quality measurements are available."* Sprint **M2.04** produced them — `docs/M2.04_RRF_Fusion_Report.md` §8, §10 — and Repository Owner ruling **RO-12** (`docs/DEFERRED_ITEMS_REGISTER.md` §4.4, Decision 3) records the outcome here. **The finding is not closed.**

**What M2-04 actually tested.** Two-route Reciprocal Rank Fusion over the semantic and lexical routes, **with no tuning of any kind**: no BM25 `k1` or `b`, no chunking change, no tokenization change, no embedding change, no `top_k` search, and no RRF `k` search against the benchmark. The mechanism M2.03-F-1 names — header-sized chunks interacting with `b = 0.75` length normalization — is therefore **untouched**, since fusion reorders candidates rather than changing which candidates BM25 produces.

**Measured on the committed 22-question Evidence Trace benchmark:**

| Metric | Historical overlap baseline | BM25 alone (M2.03) | Two-route RRF (M2.04) | Two-route candidate ceiling |
|---|---|---|---|---|
| Hit Rate | 0.7273 | 0.4091 | **0.5000** | **0.5455** |
| Chunk Precision@K (micro) | 0.1481 | 0.0833 | **0.1091** | — |
| Chunk Recall@K (micro) | 0.4324 | 0.2432 | **0.3243** | 0.3514 |

RRF recovered **28.6 %** of the Hit Rate gap to the historical baseline, **39.8 %** of the Chunk Precision@K micro gap and **42.9 %** of the Chunk Recall@K micro gap, and reached **91.7 %** of the measured two-route Hit Rate ceiling. **Ten of the 22 questions contain no expected chunk in either route's top-5 candidate pool at all.**

**The levels of claim, kept distinct for the same reason the table above keeps them distinct:**

| Level | Statement |
|---|---|
| **FACT** | RRF improved every retrieval metric the repository measures — over BM25 alone and over the semantic route alone. No measured metric moved against it |
| **FACT** | The full union of both routes' top-5 candidates has a measured Hit Rate ceiling of **0.5455**, which is itself far below the historical overlap baseline of **0.7273** |
| **CONCLUSION** | The **current two-route candidate pools are insufficient to close the historical gap through fusion alone.** Fusion improves how retrieved evidence is ordered; it cannot recover evidence that neither route places in its candidate pool |
| **HYPOTHESIS — not authority** | The remaining gap involves upstream **route quality** and/or **chunk granularity**. The ceiling measurement establishes that fusion is not the limiting factor; it does **not** establish which upstream factor dominates the remainder |

**What this disposition does not say.** It does not say M2-04 *fixed* F-1; it does not say BM25 is defective — every reported score remains independently verified against the published formula; it does not say RRF caused the remaining gap; it does not trigger **M2-15** or **M2-17**; it does not call for restoring the historical overlap scorer; and it asserts **no materiality threshold** — **no committed repository authority defines one**, and none was invented after seeing the result. Whether the measured improvement is "material" is a Repository Owner judgement this entry does not pre-empt.

**Status — PARTIALLY UNRESOLVED.** The finding remains open until the Repository Owner deliberately allocates further work or determines that the remaining gap is acceptable. **M2-15** (retrieval-quality optimization) and **M2-17** (chunk-size / granularity) remain **independently scoped** in `docs/DEFERRED_ITEMS_REGISTER.md` §4, exactly as RO-11 left them, and **neither is triggered by this disposition**. **No capability is allocated and no implementation is owed by this entry.**

### 3.6 D-2 — Contract inconsistency on `Document.id` uniqueness

| | |
|---|---|
| **Raised** | Sprint P3.1.7 (`docs/P3.1.7_Independent_Implementation_Review_ClaudeCode.md`, **MAJOR**) |
| **Verified** | Sprint P3.1.7.1 — **Independently Verified**; routed to Sprint P3.1.8 together with F-1 |
| **Previously carried in** | §3.5 (Open findings), until resolution moved it here. `docs/DOCUMENT_CONTRACT.md` §8.9 cites §3.5, the location current when Erratum E-1 was authored |
| **Evidence** | `docs/DOCUMENT_CONTRACT.md` §8.3 states `id` "is unique across the corpus"; §8.7's invariant list, declared complete, omits uniqueness. `sample_rag/knowledge_source.py` `load()` follows the weaker reading |
| **Analysed** | Sprint P3.1.8.0A (Governance Analysis) — five governance mechanisms evaluated against repository evidence; **Option A (Scoped Contract Erratum)** recommended with **Interpretation I-C** (corpus-scoped uniqueness inherited from the Knowledge Manifest, recorded adjacently) |
| **Disposition** | **RESOLVED at Sprint P3.1.8.0B** by **Contract Erratum E-1**, approved by the repository owner |
| **Implementation** | `docs/DOCUMENT_CONTRACT.md` §8.9 records the guarantee as binding, corpus-scoped, inherited from `docs/MILESTONE_1A.md` build item 1, and enforced by Data Quality Validation. §8.2–§8.8 verified byte-for-byte unchanged; `Contract Version` remains `1.0`; no ADR created |
| **Coupled to** | **F-1** (§3.1) — the invariant a DQ-2 check must enforce is now stated, satisfying the precondition that the check enforce a stated invariant rather than invent one. F-1 itself closed at Sprint P3.1.8.4 |
| **Status** | **Closed** |

### 3.7 P3.1.8.2-D1 — DQ-4 synthetic negative case not constructible under identity strategy S1

| | |
|---|---|
| **Raised** | Sprint P3.1.8.2 (Data Quality Validation Layer Review) |
| **Verified** | Sprint P3.1.8.3 (Governance & Evidence Verification) — `docs/DATA_QUALITY_VALIDATION_PLAN.md` §13's criterion *"Each check has a synthetic negative case **and** a real-corpus positive case"* is explicit and unqualified; DQ-4 is a check by §8.1 and §11.2 W5; W5 ships a synthetic **positive** case only. The criterion is therefore unmet as written |
| **Evidence** | `sample_rag/knowledge_source.py` `load()` iterates `discover_manifest_entries()`, reads `document_id = entry["id"]` and passes it through unchanged under identity strategy S1 (`docs/DOCUMENT_CONSTRUCTION_PLAN.md` §20.3; `docs/DOCUMENT_CONTRACT.md` §8.4, A5), and appends exactly one `Document` per entry. A `Document.id` with no corresponding `documents[]` entry is unreachable through repository execution at any corpus size |
| **Architectural reasoning** | DQ-4's failure state cannot be exercised without fabricating a state the repository cannot produce, which would specify a fiction rather than repository behaviour. `docs/DATA_QUALITY_VALIDATION_PLAN.md` §12's negative-case examples name a duplicate id, a stale hash, and an unmanifested file — DQ-1, DQ-2, DQ-3 — and do not name DQ-4. This is distinct from §16 open item **O-5**, which records DQ-4 as vacuously true for the separate reason of corpus scale (one document). Both limits are real and neither substitutes for the other |
| **Disposition** | **Owner-approved governance deviation, Sprint P3.1.8.4.** A literal DQ-4 synthetic negative case is determined **not constructible** under S1. The §13 synthetic-negative criterion is waived for DQ-4 alone. No artificial, synthetic, or otherwise non-representative DQ-4 negative specification is to be introduced |
| **What protects DQ-4 instead** | Regression protection against a change of identity strategy, **measured rather than asserted**: §6 records mutants **M20** (`Document.id` derived rather than read), **M21** (each entry yields two `Document`s), and **M22** (enumeration truncated) as **KILLED** by the DQ-4 specifications |
| **Scope** | DQ-4 only. DQ-1, DQ-2, and DQ-3 each retain a synthetic negative case, and §13's criterion is unmodified for them |
| **Recorded in the implementation** | `tests/test_data_quality.py` cites this section as the decision's authoritative record rather than carrying it in a module docstring — the defect class §1 records this register as having been established to end |
| **Status** | **Closed** |

---

## 4. Review dispositions

| Review | Verdict | Disposition |
|---|---|---|
| `docs/DOCUMENT_CONTRACT_REVIEW.md` (P2.5) | Outcome A — APPROVED | Findings F1, F4–F8 applied at P2.5.1; F2 → Construction Plan §9, F3 → §12 |
| `docs/P3.1.7_Independent_Implementation_Review_Codex.md` | APPROVED WITH OBSERVATIONS | Accepted except its unqualified specification-adequacy claim (§3.4, NEW-1). `P3.1.7-IMPL-01` superseded by `ADR-P3.1.7.2-F2` |
| `docs/P3.1.7_Independent_Implementation_Review_ClaudeCode.md` | CHANGES REQUESTED | Accepted, with its mutation-survivor count corrected (§3.4, NEW-2). All three required items closed at P3.1.7.2 |
| `docs/P3.1.7.1_Decision_Gate_Report_Evidence_Verification.md` | Decision Gate | Authority for every change made at Sprint P3.1.7.2 |
| `docs/P3.7.0_Manual_Review_Evidence.md` (P3.7.0) | Evidence record — not a verdict | The committed verbatim terminal transcript of the Repository Owner's manual review of commit `b50e45f`. Completes `docs/MILESTONE_1A.md` build item 10. Assessed, not re-run, by P3.7.1 |
| `docs/P3.7.1_Manual_Review_Report.md` (P3.7.1) | **Milestone 1A READY FOR Repository Governance Synchronization** | Accepted. 24 of 24 verification items PASS, 0 FAIL. Findings 1 and 2 dispositioned as confirmations of previously-diagnosed, contract-conforming accepted limitations, each linked to the authority that already records it. **No backlog item created** (§7 of that report) |

**Recorded divergence.** The two P3.1.7 reviews reached opposite verdicts. Sprint P3.1.7.1 traced the divergence to a single methodological difference — **one review measured specification adequacy; the other asserted it**. Retained as a standing lesson: an adequacy claim about a specification suite requires measurement.

---

## 5. Cross-sprint implementation observations

| Observation | Evidence | Status |
|---|---|---|
| `SUPPORTED_EXTENSIONS` is **deliberately duplicated** in `sample_rag/knowledge_source.py` and `scripts/build_manifest.py`. Centralizing would require `sample_rag/` → `scripts/` import (barred by `docs/architecture.md` §6) or a new shared module (a new architectural concept without the evidence bar `ADR-0001` applied) | Construction Plan §9.1 rated this pattern **High** drift risk when rejecting identity strategy S3 | Accepted, and now **enforced by specification** (AH-9) rather than by convention |
| No Sprint P3.1.5 commit exists; its evidence report was first committed under `5b903db`, the Sprint P3.1.6 commit, while the report's own §12 states "Repository Impact: None" | `git log --diff-filter=A` | Recorded (G-2). Documented in the report's Restoration Record |
| Construction depends on the Manifest artifact at runtime — an accepted consequence of identity strategy S1 | Construction Plan §9.1 | Accepted |
| The terminal-capture path that damaged the P3.1.5 report is still active: `docs/P3.1.7.1_Decision_Gate_Report_Evidence_Verification.md` carries 2 truncated box-drawing characters in decorative borders | Verified at Sprint P3.1.7.2 | Inert — no finding, evidence, or disposition affected. Relevant when capturing future reports |
| **No Sprint P3.3.1 commit and no Sprint P3.3.1 report exist.** The Retrieval Runtime it produced — `sample_rag/retriever.py` and `scripts/run_retrieval.py` — was first committed under `dfe1b5b`, the Sprint P3.3.2 commit. `docs/P3.3.2_Retrieval_Evaluation_Report.md` §1 records the two files as untracked at that sprint's start, *"left uncommitted because P3.3.1 also barred Git operations"* | `git log --diff-filter=A -- sample_rag/retriever.py`; P3.3.2 §1 | Recorded at Sprint P3.7.2. **Same defect class as G-2** (P3.1.5 evidence committed under the P3.1.6 commit). The runtime's own module docstring names Sprint P3.3.1, and P3.3.2 §§1, 5.4 cite its observations, so provenance is recoverable from committed artifacts — but from no single report |
| `docs/P3.3.5_Evaluation_Record_Enrichment_Report.md`'s file-impact table states 9 specifications added to `tests/test_retrieval_evaluation.py` and 8 to `tests/test_retrieval_diagnosis.py`. The measured deltas across `439e2a7`…`1b568f3` are **+8** and **+9** respectively | `diff` of `def test_` names between the two commits; totals at HEAD are 35 and 47 | Recorded at Sprint P3.7.2, **historical report unchanged**. The two counts are transposed; their sum (17) and every verdict in that report are unaffected |
| `documents[].indexed` is `false` for both catalogued documents, while `sample_rag/chunks.json` carries 172 chunks derived from them and `docs/P3.3.5_…` §4 reasons that the expected document *"demonstrably **is** indexed"* | `sample_rag/knowledge_manifest.json`; `docs/MILESTONE_1A.md` build item 1 schema; `docs/P3.3.5_…` §4, §5 | Recorded at Sprint P3.7.2. The field's semantics under a chunk-only Index stage are unresolved; regeneration of the Manifest is barred by that sprint's scope. Carried as a deferred item in `docs/P3.7.2_Repository_Governance_Synchronization_Report.md` §5 |
| The **Index Layer** named by `docs/MILESTONE_1A.md` build item 3 — deterministic placeholder vectors behind an `EmbeddingProvider` interface — does not exist. No `Indexer`, `EmbeddingProvider` or `VectorStore` is defined anywhere in the repository; the only occurrence of the name is a docstring in `tests/test_data_quality.py` recording the blocker | `grep -rn "EmbeddingProvider" --include=*.py`; `docs/DATA_QUALITY_VALIDATION_PLAN.md` §8.1 (DQ-7), §11.2 W6, §16 O-6 | **Previously recorded**, by the DQV plan, as the blocker for DQ-7 and for build item 2's Index Coverage Validation clause. Re-verified unchanged at Sprint P3.7.2 and carried in that sprint's Deferred Repository Items Register |
| **`scripts/evaluate_context.py` is no longer *untouched*, and the two governance statements that say it is remain correct as historical records.** `docs/DEFERRED_ITEMS_REGISTER.md` §4.10 **RO-18 Decision 11** and §2's **RO-18 D-11** row above both record the file as untouched *at commit `6543bae`*, which it was. (`docs/M2.08_Native_Generation_Metrics_Report.md` §1 also lists the file as byte-identical, but scoped to what **commit `09fda24`** modified, and that statement stays true without qualification.) Commit **`42bcadb`** — *"add safe CLI selection guard to M2-07 evaluator"* — subsequently modified it: an explicit `--case-id ID` / `--all` selection is now required, and the two expensive imports (`sample_rag.vector_runtime`, `scripts.run_hybrid_retrieval`) are deferred until a selection has been validated, so `--help`, a missing selection, an unknown argument and an unknown case id are all refused before any model, index or provider object exists | `git show 42bcadb --stat` — `scripts/evaluate_context.py`, `tests/test_evaluate_context_cli.py`, and no other file. `git diff 6543bae..HEAD -- evaluation/context_metrics.py tests/test_context_metrics.py docs/M2.07_Native_Context_Metrics_Report.md` is **empty**. The executable bodies of `load_reference_entries`, `retrieve_cases`, `partition` and `report_scores` are identical across `6543bae`…`42bcadb` once the relocated imports are excluded | Recorded after commit `42bcadb`, **historical statements unchanged**. **The change is to the invocation surface only** — **Context Precision / Context Recall semantics, the retrieval measured, and the metric engine are all unchanged**, and **M2-07 remains OPEN**, unreopened and unrerun. An adjacent note is carried at `docs/DEFERRED_ITEMS_REGISTER.md` §4.10 Decision 11; the RO-18 ruling itself is neither amended nor renumbered |
| **`docs/M2.07_Native_Context_Metrics_Report.md` §10 documents a flagless invocation that the evaluator now refuses.** It reads *"One full run of `python3 -m scripts.evaluate_context`"*, which was the whole interface at commit `6543bae`. Since commit **`42bcadb`** that invocation exits **2** with a usage error; the current forms are `PYTHONPATH=. python3 -m scripts.evaluate_context --case-id <GOLDEN_ID>` for exactly one committed golden entry, and `PYTHONPATH=. python3 -m scripts.evaluate_context --all` as the only authorization of the full 22-entry dataset; the two are mutually exclusive and neither is a default | `scripts/evaluate_context.py` `build_parser` / `select_entries`; `tests/test_evaluate_context_cli.py` (35 specifications, including the refusal and mutual-exclusion paths) | Recorded after commit `42bcadb`, **historical report unchanged** under **CP-3** — *"corrections are recorded forward."* **The §10 measured results are unaffected and are not restated, re-derived or rerun**, and **no evaluation was executed to record this observation**. The report remains the accurate account of the run it describes; this row is where a reader resolves the invocation |
| **`docs/M2.08_Native_Generation_Metrics_Report.md` §11 describes a whole-dataset run the evaluator no longer performs unprompted.** It reads *"`scripts/evaluate_generation.py` evaluates the whole golden dataset in one run, which is roughly **22 generation calls plus ~450 judge calls**"* — accurate as of commit **`09fda24`**, at which the entry point took no arguments at all and the full dataset was its only behaviour. Commit **`6fe2337`** — *"fix(evaluation): add safe CLI selection guard to M2-08 evaluator"* — subsequently made that run **conditional on an explicit `--all`**. Current forms: `python3 -m scripts.evaluate_generation --case-id <GOLDEN_ID>` evaluates exactly one committed golden entry; `python3 -m scripts.evaluate_generation --all` is the only authorization of the full 22-entry dataset; the two are **mutually exclusive**; and an invocation carrying **no selection is refused with exit `2`** | `git show 09fda24:scripts/evaluate_generation.py` contains no `argparse` import and no flag; `git show 6fe2337 --stat` — `scripts/evaluate_generation.py` and `tests/test_evaluate_generation_cli.py`, and no other file. Verified at HEAD without a provider call: `build_parser()` exposes exactly `{-h, --case-id, --all}` with `--case-id` and `--all` in one mutually-exclusive group (`--case-id X --all` exits `2`, *"not allowed with argument"*); `select_entries` raises `SelectionError` for no selection and for an unknown id, resolving `--all` to **22** entries and a valid `--case-id` to **1**; a bare credential-free invocation exits **2** with *"a selection is required"* | Recorded after commit `42bcadb`, **historical report unchanged** under **CP-3** — *"corrections are recorded forward."* **The report's §11 headline finding is unaffected and is not restated, re-derived or rerun**: it records that *"the full 22-case live evaluation was NOT executed"*, and that remains true — the guard makes the run harder to start by accident, it does not change what that sprint measured, and the one live boundary-reuse call §11 does report is untouched by it. **No evaluation was executed to produce this observation** — no `--all` run, no `--case-id` run, no provider call — and the report itself is **not edited**. This row is where a reader resolves the invocation |

---

## 6. Specification adequacy baseline

Recorded so the figure is not re-cited without its qualification (finding G-4).

**Mutation baseline — Sprint P3.1.7.1**, 18 semantically meaningful mutations against commit `5b903db`: 12 killed, 6 survived. Sprint P3.1.7.1 established that the survivor count **overstated** the gap:

| Survivor | Classification |
|---|---|
| `PARAGRAPH_SEPARATOR "\n\n"→"\n"` | Genuine blind spot → closed by AH-2 |
| `paragraph.iter(w:t)`→`findall` | Genuine blind spot → closed by AH-1 |
| reject empty text in `load()` | Unspecified legality → closed by AH-3 |
| remove unreachable non-`str` guard | Dead code; survival expected → resolved by AH-8 |
| `newline=""` removed from `extract_text` | **Equivalent mutant** — final `Document.text` is byte-identical; only the intermediate raw string differs |
| `body.iter(w:p)`→`findall` | **Equivalent mutant on the current corpus** — the resume has 0 nested paragraphs (86 via both). Becomes a genuine gap only if the corpus gains tables or text boxes |

**Post-remediation, Sprint P3.1.7.2:** every genuine mutant is killed. The only survivors are the two equivalent mutants above, whose survival is correct behaviour.

**Mutation baseline — Data Quality Validation, Sprint P3.1.8.4**, 23 semantically meaningful mutations against commit `3a32253`, satisfying `docs/DATA_QUALITY_VALIDATION_PLAN.md` §12 (*"must run a mutation pass over any code its checks protect and record the result"*) and §13's Evidence criterion.

**Scope**, fixed before execution and not broadened: `scripts/build_manifest.py` — `load_manifest`, `validate_manifest`, `compute_sha256`, `discover_documents`, `normalize_source_path`; `sample_rag/knowledge_source.py` — `resolve_source_path`, `load()`. Every mutation lies inside one of those seven function bodies. Mutants were applied to a throwaway copy of the repository, one at a time, with the source restored between runs; the working tree was never mutated.

Each mutant was run twice — against the **DQV suite alone** (`tests/test_data_quality.py`) and against the **full suite** — so protection attributable to W1–W5 is distinguished from protection the repository already had:

| | Count |
|---|---|
| Mutants generated | **23** |
| Killed by the DQV suite alone | **11** — M01, M04, M06, M07, M09, M10, M13, M18, M20, M21, M22 |
| Killed by the full suite | **14** — the 11 above, plus M17, M19, M23 |
| Survived both | **9** |
| — of which **equivalent mutants** | **5** |
| — of which **genuine survivors outside DQV's scope** | **4** |
| — of which **genuine survivors inside DQV's scope** | **0** |

| Survivor | Function | Classification |
|---|---|---|
| M02 `path.suffix.lower()` → `path.suffix` | `discover_documents` | **Equivalent on the current corpus** — every corpus file and every synthetic fixture carries a lowercase extension. A genuine gap only if a file with an uppercase extension enters the corpus |
| M03 hidden / `__pycache__` skip removed | `discover_documents` | **Equivalent on the current corpus** — `sample_rag/documents/**` contains no hidden or `__pycache__` path |
| M05 `as_posix()` → `str(...)` | `normalize_source_path` | **Equivalent on POSIX** — the two agree when the OS separator is `/`; the suite runs on Linux. A genuine gap on Windows |
| M08 read chunk size 8192 → 1 | `compute_sha256` | **Equivalent, unconditionally** — SHA-256 is a streaming digest; chunking changes performance, never the value |
| M11 read encoding `utf-8` → `latin-1` | `load_manifest` | **Equivalent on the current artifact** — `knowledge_manifest.json` is pure ASCII, where the two decodings coincide. A genuine gap if a non-ASCII byte enters the Manifest |
| M12 JSON parse failure no longer wrapped | `load_manifest` | **Genuine survivor, outside DQV's scope.** Manifest load/parse failure is a `ManifestValidationError` concern, listed at plan §8.2 under *"Explicitly **not** DQV failure classes"* and owned by Structural Artifact Validation |
| M14 `manifest_version` equality check removed | `validate_manifest` | **Genuine survivor, outside DQV's scope** — same §8.2 assignment |
| M15 per-entry field type check removed | `validate_manifest` | **Genuine survivor, outside DQV's scope** — same §8.2 assignment |
| M16 missing-`documents` guard removed | `validate_manifest` | **Genuine survivor, outside DQV's scope** — same §8.2 assignment |

**No genuine survivor lies inside a DQV-owned behaviour.** The four genuine survivors are all manifest *structural* failures, which plan §8.2 assigns to Structural Artifact Validation and §11.2 records against W1 as failure classes *"—"*. They are a measured confirmation that W1 is a gate call and not a structural-failure specification, not a DQV blind spot.

**Boundary confirmation, in the other direction.** Three mutants were killed by the full suite but survived the DQV suite, each correctly:

| Mutant | Survived DQV because | Killed by |
|---|---|---|
| M17 corpus-root containment removed | Containment is Construction's, per `ADR-P3.1.7.2-F2`; plan §5.5 and §11.3 bar DQV from specifying it | AH-7 |
| M19 extension gate removed | Construction's single-entry admissibility gate (§6.1 row 2) | Construction failure surface |
| M23 manifest order reversed | Ordering is Construction Behaviour; §11.3 bars DQV from re-specifying it. W5 asserts cardinality, not order | B9 / B10 |

**Protection attributable to each work package**, by killed mutant: **W1** — M13 (the gate must return the same object, not a copy). **W3 / DQ-1** — M07, M09, M10, M18. **W4 / DQ-3** — M01, M04, M06. **W2 / DQ-2** — M18, M20, M21, M22. **W5 / DQ-4** — M18, M20, M21, M22.

**One measured correction to a recorded limitation.** `tests/test_data_quality.py` records W2 as vacuously true on the one-document corpus. M21 (`load()` appends two `Document`s per entry) is killed by `test_dq2_loaded_document_ids_are_pairwise_distinct`. The predicate is therefore vacuous against a duplicate *Manifest entry* — the F-1 shape — but not against a `load()` that duplicates a `Document`. The recorded limitation is narrower than stated, in the repository's favour.

**Line coverage:** `sample_rag/document.py` 100%, `sample_rag/knowledge_source.py` 100%.

### 6.1 Later mutation rounds — added at Sprint P3.7.2

Recorded from the sprint reports that produced them. Each figure is that sprint's own; none is re-derived here, and none is a repository-wide adequacy claim.

| Sprint | Scope | Mutants | Killed | Survived | Recorded in |
|---|---|---|---|---|---|
| **P3.4.1** — Dataset authority validation | Golden Dataset, QA pairs, Evidence Trace, cross-dataset integrity, mutated in memory | 34 | **34** | 0 | `docs/P3.4.1_Dataset_Authority_Validation_Report.md` §Mutation evidence |
| **P3.5.2** — Generation runtime | `sample_rag/generator.py`, via mutant results and mutant `Generator` subclasses in memory | 25 | **25** | 0 | `docs/P3.5.2_Generation_Implementation_Report.md` §Mutation evidence |
| **P3.6.0** — CLI integration | `scripts/cli.py`, structural mutants against the module source and behavioural mutants executed as real processes | 25 | **25** | 0 | `docs/P3.6.0_CLI_Integration_Report.md` §Mutation evidence |

**No repository file was modified during any of the three rounds** — each report records the isolation mechanism it used.

**Two consecutive rounds found a defect in a specification rather than in an implementation**, and both were fixed and re-verified before the sprint closed: P3.5.2's §12 varying-value scan missed the `created_at` key (killed independently by the exact-key `diagnostics` check, so coverage never had a hole); P3.6.0's authority-import check was written to skip docstrings and was thereby skipping the imports it existed to inspect, and its orchestration check accepted any assignment binding a call. Recorded because it qualifies the figures above in the direction the standing caution already warns about — **the validation layer is itself under test, and twice it was the thing that failed.**

**No mutation round was performed for Sprints P3.3.2, P3.3.3, P3.3.4 or P3.3.5.** Those sprints used **two-path independence** instead — an engine and a validator that share no import, each computing the same quantity by a different route (`docs/P3.3.3_…` §Validation, `docs/P3.3.4_…` §Independence), with the independence itself enforced by AST specifications. That is a different instrument, not a weaker application of the same one, and it is not interchangeable with a mutation figure.

**Standing caution:** a surviving mutant is evidence of a blind spot; a killed mutant is not proof of adequacy. Line coverage is not evidence of behavioural protection — at Sprint P3.1.7.1 the module measured 99% while a mutation emptying its entire output went undetected.

---

## 7. Claim-to-specification mapping

**372 specifications at commit `d9a6db4`**, collected read-only at Sprint P3.7.2 and matching the count `docs/P3.7.0_Manual_Review_Evidence.md` lines 1–5 record as passing. The register carried **109** when it was written at Sprint P3.1.7.2 — 17 pre-existing (`test_chunker.py`), 78 Knowledge Layer, 14 Data Quality Validation — and §7.1 records the 263 added since, by owning sprint.

The table below is unchanged from establishment and continues to map the original 109.

| Claim family | Source | Specifications | File |
|---|---|---|---|
| Runtime Contract A1–A16 | P3.1.5 Layer A | 22 | `tests/test_document_contract.py` |
| Construction Behaviour B2–B10, invariant 3 | P3.1.5 Layer B | 16 | `tests/test_knowledge_source_construction.py` |
| Failure surface (18 modes, Case B, error-type independence) | P3.1.5 §8 | 22 | `tests/test_knowledge_source_failures.py` |
| **AH-1** `Document.text` content | Decision Gate EV-2, I-4 | 4 | `tests/test_knowledge_source_construction.py` |
| **AH-2** rule N1 and Chunk-boundary chain | Decision Gate EV-1 | 3 | `tests/test_knowledge_source_construction.py` |
| **AH-3** empty-text legality | Decision Gate I-5 | 4 | `tests/test_knowledge_source_construction.py` |
| **AH-7** corpus-root containment | `ADR-P3.1.7.2-F2` | 6 | `tests/test_knowledge_source_failures.py` |
| **AH-9** admissibility-gate agreement | Decision Gate A-1 | 1 | `tests/test_knowledge_source_failures.py` |
| **W1** Manifest structural gate | DQV Plan §11.2; `docs/MILESTONE_1A.md` build item 1 | 3 | `tests/test_data_quality.py` |
| **W2 / DQ-2** identifier uniqueness (F-1) | DQV Plan §11.2; `docs/DOCUMENT_CONTRACT.md` §8.9 (Erratum E-1) | 4 | `tests/test_data_quality.py` |
| **W3 / DQ-1** freshness / integrity | DQV Plan §11.2; `docs/MILESTONE_1A.md` build item 1, Architectural AC 3 | 2 | `tests/test_data_quality.py` |
| **W4 / DQ-3** completeness — Case A | DQV Plan §11.2, §8.3; `docs/DOCUMENT_CONSTRUCTION_PLAN.md` §20.3 | 2 | `tests/test_data_quality.py` |
| **W5 / DQ-4** referential integrity | DQV Plan §11.2; `docs/DOCUMENT_CONTRACT.md` §8.5 (A8) | 3 | `tests/test_data_quality.py` |

**Not specified, deliberately:** symlink containment (§3.5, F-2-sym); `Document` persistence and structural validation (unresolved by the contract); a DQ-4 synthetic negative case (§3.7, **P3.1.8.2-D1** — owner-approved governance deviation).

### 7.1 Specifications added after establishment — recorded at Sprint P3.7.2

263 specifications across six families, each attributed to the sprint that shipped it. Counts are the collected count at `d9a6db4`; where a sprint report states a different figure, both are shown and the divergence is recorded in §5.

| Claim family | Owning sprint(s) | Specifications | File |
|---|---|---|---|
| **Retrieval evaluation** — classification algebra, pairing, validation checks, committed corpus | P3.3.2 (27), **+8** at P3.3.5 for document identity | **35** | `tests/test_retrieval_evaluation.py` |
| **Retrieval metrics** — metric algebra, input contract, two-path independence, committed corpus | P3.3.3; unchanged at P3.3.5 | **35** | `tests/test_retrieval_metrics.py` |
| **Retrieval diagnosis** — rule transcription against `docs/altm.md`, rule selection, confidence, dependency rule, independence, committed corpus | P3.3.4 (38), **+9** at P3.3.5 | **47** | `tests/test_retrieval_diagnosis.py` |
| **Golden Dataset** — GD-1 … GD-13 | P3.4.1 | **16** | `tests/test_golden_dataset.py` |
| **QA pairs** — QA-1 … QA-10 | P3.4.1 | **14** | `tests/test_qa_pairs.py` |
| **Evidence Trace Dataset** — ET-1 … ET-6 | P3.4.1 | **25** | `tests/test_evidence_trace_dataset.py` |
| **Cross-dataset integrity** — X-1 … X-16 | P3.4.1 | **16** | `tests/test_cross_dataset_integrity.py` |
| **Generation** — `docs/GENERATION_CONTRACT.md` §16, guarantees G-1 … G-14, every guarantee mapped | P3.5.2 | **48** | `tests/test_generator.py` |
| **CLI integration** — contract behaviour, byte-identity, exit codes, and six AST specifications constraining what the module may contain and import | P3.6.0 | **27** | `tests/test_cli.py` |

**Reconciliation:** 109 (§7) + 263 (§7.1) = **372**, the collected total at `d9a6db4`. Sprint P3.4.1's four dataset families complete `docs/MILESTONE_1A.md` build item 9.

**Two claim families in §7.1 are enforced structurally rather than behaviourally** and are recorded as such so the distinction is not lost: the CLI's *"contains no business logic"* property and the metrics/diagnosis validators' import independence are AST-parsed allowlist specifications. A behavioural test cannot observe a branch being added; these can.

---

## 8. Maintenance

- Add a row only **after** a disposition is established.
- **Finding closure is event-driven** (policy adopted at Sprint P3.1.8.4). A finding closes when the implementation trigger its own entry documents occurs — not when the sprint carrying that work is judged complete. Sprint completion is governed independently, by that sprint's own acceptance criteria. Applied first to F-1 (§3.1).
- Every row cites repository evidence — a file, a commit, a measurement, or an ADR.
- A finding is never removed. It moves to Closed, with the change that closed it.
- If a row would describe work to be scheduled, it belongs in `docs/roadmap.md` instead (§1.2).
- **Governance synchronization does not create dispositions.** Sprint P3.7.2 added §2's non-ADR disposition table, two §4 review rows, four §5 observations, §6.1 and §7.1 — every one of them a record of something a committed authority had already established. Where P3.7.2 found deferred work, it recorded it in its own Deferred Repository Items Register, **not here**: a deferred item awaiting a Repository Owner sequencing decision is scheduled work, which §1.2 bars from this register.
